#set($hasDateTime = false)
#set($hasDecimal = false)
#foreach($col in $table.allColumns)
#if($col.javaShortType == "LocalDateTime" || $col.javaShortType == "LocalDate")
#set($hasDateTime = true)
#elseif($col.javaShortType == "BigDecimal")
#set($hasDecimal = true)
#end
#end
package ${package}.domain.entity;

#if($hasDateTime)
import java.time.LocalDate;
import java.time.LocalDateTime;
#end
#if($hasDecimal)
import java.math.BigDecimal;
#end
import com.baomidou.mybatisplus.annotation.TableName;
import lombok.Data;
import lombok.EqualsAndHashCode;
import lombok.NoArgsConstructor;
import lombok.experimental.SuperBuilder;

import com.njydsz.common.jdbc.entity.MpBaseEntity;

/**
 * ${table.tableComment}实体
 *
 * <p>对应数据库表 ${table.tableName}。
 *
 * @author ${author}
 * @since ${date}
 */
@Data
@SuperBuilder
@NoArgsConstructor
@EqualsAndHashCode(callSuper = true)
@TableName("${table.tableName}")
public class ${table.entityName} extends MpBaseEntity<String> {

#foreach($col in $table.columns)
#if($col.comment && $col.comment != "")
  /** ${col.comment} */
#end
  private ${col.javaShortType} ${col.javaFieldName};
#end
}
