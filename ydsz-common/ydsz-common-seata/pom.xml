<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.njydsz</groupId>
        <artifactId>ydsz-common</artifactId>
        <version>26.09.01-SNAPSHOT</version>
    </parent>

    <artifactId>ydsz-common-seata</artifactId>
    <name>ydsz-common-seata</name>
    <description>YDSZ 分布式事务封装（Seata 全局事务、XID 透传、健康检查）</description>

    <!--
      ============================================================
      设计说明
      ============================================================
      1. 本 pom 仅依赖 common 内部模块 + Spring 基座（AOP / WebMVC /
         WebFlux / Servlet），版本由父 pom 的 Boot BOM（4.1.0）+
         Cloud BOM（2025.1.2）统一管理。

      2. Seata 客户端（seata-spring-boot-starter / seata-all /
         spring-cloud-starter-alibaba-seata）由消费业务模块按需引入，
         版本通过 spring-cloud-alibaba BOM（2025.1.0.0）管控。
         业务方引入 Seata 客户端后，SeataAutoConfiguration /
         YdszGlobalTransactionalAspect / XidServletFilter /
         FeignXidRequestInterceptor 通过反射桥接生效；未引入时
         本模块的 bean 不会被注册，业务代码无感知。

      3. 拦截器 / 过滤器 / 健康检查由
         @ConditionalOnProperty("ydsz.seata.enabled") 门控；
         XID 透传必填 classpath 存在 io.seata.core.context.RootContext
         （即业务方已引入 Seata 客户端），否则
         XidServletFilter / FeignXidRequestInterceptor 也不会注册。

      4. 架构规范
         - 全局事务由 Aspect 驱动的 @Around 通知实现；需通过
           io.seata.spring.annotation.GlobalTransactionScanner 扫描
           开启全局事务。本模块不直接 import org.apache.seata.*，
           通过反射桥接，满足 YDIZ-COMMON-003 / 22.4 / §25.1 的要求。
         - ydzz.seata.* 前缀仅控制本模块的行为开关；Seata 客户端自身的
           seata.*（TC 地址、vgroup 等）仍由业务 application.yml 配置。
      ============================================================
    -->

    <dependencies>
        <!-- common 基础层 -->
        <dependency>
            <groupId>com.njydsz</groupId>
            <artifactId>ydsz-common-core</artifactId>
        </dependency>
        <dependency>
            <groupId>com.njydsz</groupId>
            <artifactId>ydsz-common-util</artifactId>
        </dependency>
        <dependency>
            <groupId>com.njydsz</groupId>
            <artifactId>ydsz-common-exception</artifactId>
        </dependency>

        <!-- Spring 基座 -->
        <!-- spring-boot-starter-aspectj（AspectJ 织入，用于 YdszGlobalTransactionalAspect；
             不可用 spring-boot-starter-aop，该 artifact 不在内部 Boot BOM 中） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-aspectj</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-webmvc</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>jakarta.servlet</groupId>
            <artifactId>jakarta.servlet-api</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-commons</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-openfeign</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- 工具 -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>
        <!-- spring-boot-health（HealthIndicator / HealthContributor / Status；
             Boot 4 将 actuator health 拆到 spring-boot-health，故不可使用 -actuator） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-health</artifactId>
            <scope>provided</scope>
        </dependency>
    </dependencies>
</project>
