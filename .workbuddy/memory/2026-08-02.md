# 2026-08-02 Work Log

## ydsz-common-json 对标竞品优化

### 本轮新增（Session 5）

**P2-C2 消除 round-trip 转换**:
- JsonMapper.valueToTree(): 加 Map/List/JsonNode/简单值 直接 TreeConverter 快速路径，免 String 中转
- JsonMapper.convertValue(): 加同类型早返回 + JsonNode→POJO 走 treeToValue 管道
- YdszJson.valueToTree(): 同上优化

**P2-B5 Record 反序列化**: 验证已完备实现（BeanDeserializerEngine.deserializeRecord + convertRecordValue），报告误报项修正

**P2-D5 异常定位**: 验证已完备（JsonDeserializationException 含 getLine/getColumn/getContextSnippet/computeLineColumn）

**P1 Bug 修复**:
- ObjectNode.asValue(): Collectors.toMap→LinkedHashMap 手动循环，消除 null value NPE
- JsonSchema.enumValues(): Arrays.asList→new ArrayList，消除 add() 抛出 UnsupportedOperationException
- JsonSchema.boolean_(): 删除重复方法

**P2-B4 JsonPointer 可组合**: 新增 append(segment)/tail()/head()/depth() 方法

**P2-B3 JsonPath 标准函数**: 新增 length(json,path)/min(json,path)/max(json,path)/sum(json,path)

### 累计完成汇总

| 维度 | 完成项 |
|---|---|
| **P0** | ASM 前缀修复、COMPUTE_FRAMES 调整、wrapRootValue/failOnError/monitoring 修复、测试体系 9/11 绿 |
| **P1-安全** | AutoType 数组绕过+内部类递归、4/4 安全测试全绿 |
| **P1-缓存** | LruSoftCache/LruCache→ConcurrentHashMap |
| **P1-体验** | namingStrategy Converter、健康检查、metadata 修正 |
| **P1-Bug** | ValidationResult、TextNode 转义、JsonPatch 字段检测、ObjectNode NPE、Schema enumValues |
| **P1-减负** | isEnabled、JsonTypeCode、ObjectReader、orderedModules、api.Serializer/Deserializer @Deprecated、boolean_() 删除 |
| **P1-架构** | FieldTypeCode 统一枚举、JsonObject/JsonArray @Deprecated |
| **P2-功能** | JsonPointer append/tail/head/depth、JsonPath length/min/max/sum、JsonSchema uniqueItems/minProperties/maxProperties、Record 反序列化验证 |
| **P2-性能** | valueToTree/convertValue round-trip 快速路径 |
| **P2-修复** | JsonPath 基本类型数组 ClassCastException、文件损坏修复(BeanDeserializerEngine/FieldMeta/ZeroCopyDeserializer/AsmBeanCodecGenerator 共 15+处) |

### 待续

- ASM 帧 bug（NegativeArraySizeException，需升级 ASM 或修复字节码生成模式）
- P1-A2 去全局化（SerializationProvider 静态→实例化配置传参，20-40 文件）
- P2-B1 多态 EXISTING_PROPERTY/EXTERNAL_PROPERTY
- P2-C3/C5 真流式输出/输入
- P2-B6 @JsonEnumDefaultValue、@JsonFormat.locale/timezone
