# 2026-08-03 工作日志

## 移除项目中所有 @Deprecated 弃用代码并更新引用

**修改的文件（ydsz-backend）：**
1. `ydsz-common-util/.../security/PwdUtils.java`：删除 5 个弃用方法（verifyPasswordWithSha256Salt / isValidPasswordWithSalt / encodeWithSalt / encodeWithAutoSalt / getDefaultPassEncryption），更新类注释与日志文案
2. `ydsz-common-util/.../security/DigestUtils.java`：删除 6 个 MD5/SHA-1 弃用方法（md5、md5Hex×2、sha1、sha1Hex×2），保留通用 digest() 供 FileUtils 使用
3. `ydsz-common-util/.../security/AesUtils.java`：删除 decryptECBCompat / decryptCBCCompat 及 CBC/ECB 常量，清理 import、@Slf4j，更新类 Javadoc
4. `ydsz-common-util/.../http/CookieUtils.java`：删除 7 参数弃用 addCookie（无外部调用，8 参数 SameSite 版为替代）
5. `ydsz-common-util/.../security/Rsa2Utils.java`（顺带修复预先存在的 bug）：补 `import java.security.SignatureException`
6. `ydsz-nextwiki/.../DownloadRateLimitService.java`：`DigestUtils.md5Hex` → `sha256Hex`（用的是 Apache Commons Codec 的 DigestUtils，API 兼容）
7. `DigestUtilsTest.java`：删除 md5KnownVector 测试用例

**编译验证：**
- `mvn -pl ydsz-common/ydsz-common-util -am compile -Dmaven.test.skip=true` → BUILD SUCCESS
- `mvn -pl ydsz-nextwiki/ydsz-nextwiki-server compile -Dmaven.test.skip=true` → BUILD SUCCESS

**环境备忘：**
- Maven 在 `D:\Maven\remi-soft-maven`，但 `mvn` 脚本在 Git Bash 下有 classworlds 报错，需手动执行：
  `java -classpath "D:/Maven/remi-soft-maven/boot/plexus-classworlds-2.9.0.jar" -Dclassworlds.conf=... -Dmaven.home=... org.codehaus.plexus.classworlds.launcher.Launcher <args>`

**发现的项目预先存在问题（与本任务无关，git 干净状态）：**
- `HashUtilsTest.java` 引用 HashUtils 中不存在的 Base58/consistentHash API → testCompile 失败
- `OpenApiAutoConfiguration.java`（common-base）引用 DocProperties 不存在的 getter 且 Lombok log 找不到 → 编译失败
- 注意：common-util 目录存在并发修改（他人/其他会话在增强 RSA2、Cookie 安全等），本任务期间文件多次变化

**Maven 手动调用模板（Git Bash）：**
```bash
MVN_JAR="D:/Maven/remi-soft-maven/boot/plexus-classworlds-2.9.0.jar"
MVN_HOME="D:/Maven/remi-soft-maven"
cd D:/Code/ydsz/ydsz-pmis/ydsz-backend
java -classpath "$MVN_JAR" "-Dclassworlds.conf=$MVN_HOME/bin/m2.conf" "-Dmaven.home=$MVN_HOME" "-Dlibrary.jansi.path=$MVN_HOME/lib/jansi-native" "-Dmaven.multiModuleProjectDirectory=$(pwd)" org.codehaus.plexus.classworlds.launcher.Launcher <mvn参数>
```
