# 2026-08-03

## ydsz-frontend 全面代码评审
- 完成 ydsz-frontend（Qiankun + Vue3 + Element Plus 微前端 monorepo，9 子应用 + 1 主应用）全面评审，产出报告 `docs/FRONTEND_OPTIMIZATION_REVIEW.md`
- 关键发现：架构/工程化达大厂水准；最大短板是测试（34 个单测全在 comm/conf，9 个业务应用 0 测试，e2e 仅 5 用例且 if 包裹可静默跳过）
- 重要过度设计：secure-ls 加密 localStorage 存 token 是伪安全（密钥随 bundle 分发），建议改 HttpOnly Cookie；自研 hover prefetch（bootstrap.ts 137-227 行）建议删；openapi-generator 配置存在但从未落地（无 sdk 产物）
- 性能：压缩未开（compress:false）、无图片优化、CDN 走 esm.sh 公共服务（建议内化到自有 Nginx）
- 类型：tsconfig strict 全开但 522 处 any，eslint 主动关闭 no-explicit-any
- 项目结构事实：workspace = apps/*(9) + main + comm(@core/effects/基础包) + conf(lint/vite/tsconfig)；pnpm catalog 统一依赖；部署为 Nginx 单容器托管全部子应用 /ydsz-*-web/ 子路径

## 自研微前端框架方案
- qiankun 直接引用仅 4 处：main/src/bootstrap.ts、main/src/qiankun/{index,global-state}.ts、comm/effects/shared-auth/src/create-sub-app.ts；9 子应用业务代码零感知（全部走 createSubApp 工厂）
- 用户已确认方向：自研接口层 + 自研轻内核（非迁移 wujie/micro-app）
- 产出方案文档 `docs/MICRO_RUNTIME_AND_CAPABILITY_PLAN.md`：核心洞察是 9 应用同团队同构建链 → 可跳过资源加载器（ESM manifest + dynamic import）与 JS 沙箱（scoped 样式隔离即可），工作量 4-5 周
- 包规划：@ydsz/micro-runtime（接口层）+ @ydsz/micro-kernel-lite（轻内核）+ @ydsz/micro-adapter-qiankun（兜底），均放 comm/effects
- 里程碑：M1 接口层(1周) → M2 轻内核(1.5周) → M3 保活/事件总线(1周) → M4 按 agent→system→… 灰度(1周)

## 今日代码落地
- M1 完成：@ydsz/micro-runtime 接口层（types / global-state / event-bus / create-runtime / composable，7 文件）
- M1 完成：@ydsz/micro-adapter-qiankun（将 qiankun registerMicroApps 包装为 MicroRuntime 实现）
- M1 完成：main/src/bootstrap.ts 重构（registerQiankun → createRuntime + registerKernel，qiankun 引用收敛至 adapter）
- M1 完成：create-sub-app.ts 增加 defineSubApp 双兼容导出（qiankun + lite-kernel 同一产物）
- M2 完成：@ydsz/micro-kernel-lite 轻内核（loader / scheduler / error-boundary / lite-kernel / vite-plugin-manifest，6 文件）
- C2 完成：v-safe-html 指令（common-ui/src/components/safe-html），替换 5 处裸 v-html
- C3 完成：compress:true 开启 gzip/brotli 构建压缩；nginx.conf 加 gzip_static/brotli_static + CSP report-only
- C4 完成：eslint no-explicit-any: off → warn
- C5 完成：e2e 去 if(isVisible) 软断言，测试账号改环境变量 E2E_TEST_USERNAME/PASSWORD
