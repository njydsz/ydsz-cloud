# 2026-08-04

## ydsz-common-core 全局引用分析
- 完成对 ydsz-common-core 模块的全项目引用分析，生成报告: docs/ydsz-common-core-reference-analysis.md
- 扫描了全部 10 个业务模块 + Gateway，统计 ~296 个 Java 文件引用 core
- 核心发现:
  - **P0**: 双轨错误码体系 (ResultCode vs ExceptionCode)，业务模块全部绕过 BaseResultCode
  - **P0**: Controller 层 85% error() 调用不带错误码
  - **P1**: 两套 ProblemDetail 实现、三套租户上下文
  - **P2**: BaseDTO/BaseQuery/TreeNode/BaseStatusEnum/DomainEvent 等公共基类未被业务模块使用
  - BaseResponse 和 TraceId 机制使用非常规范，100% 统一
- 提出三阶段优化建议: 止血(统一错误码体系) → 打通(合并ProblemDetail/租户上下文) → 深化(推广DDD基类/领域事件)

## 按优先级逐项优化实施
- 全部 P0/P1/P3 项已完成，累计变更 59 个文件、83+ 处修改:
  - P0-1: ExceptionCode extends ResultCode，统一双轨错误码体系
  - P0-2: BaseResultCode 11 个业务码标记 @Deprecated
  - P0-3: 25 个 Controller 文件 62 处 error() 补全错误码
  - P1-1: 合并两套 ProblemDetail 为增强版 core 版，删除 exception 版
  - P1-2: TenantContextHolder 双向同步 RequestContext
  - P1-3: Gateway 8 处硬编码数字 → BaseResultCode 枚举
  - P3-4: 17 个 -web/-server pom.xml 添加显式 core 依赖
  - P3-1/2: 编写 DDD 建模基类使用指南 docs/ddd-common-patterns-guide.md
  - 产出: docs/ 下 2 份报告 + 59 个文件修改

## 第二轮优化：消除剩余硬编码 + ArchUnit + DDD 基类落地
- 修复 NotificationClientFallbackFactory 4处 "B01004" → BaseResultCode.SERVICE_UNAVAILABLE
- 修复 DebugController 2处 "TRACE_NOT_FOUND"/"TRACE_EMPTY" → AgentResultCode 新增 B94401/B94402
- 修复 RepeatSubmitTokenController "401" → BaseResultCode.UNAUTHORIZED
- 新增 ArchUnit R30 规则：禁止 Controller 中 BaseResponse.error(String) 单参调用
- ProjectInitiationPostDTO 继承 BaseDTO（示范 DDD 基类继承模式）
- MessageLogQueryDTO 移除冗余 tenantId 字段（已由 BaseQuery 提供）
- 全量扫描确认：0 个硬编码数字错误码残留

## 移除弃用代码
- 从 BaseResultCode 删除 11 个已弃用业务码：B3xxxx(用户) 7个 + B7xxxx(工作流) 3个 + 已弃用的密码错误码共 11 个
- 更新 BaseResultCodeTest：6 处断言替换为等价的系统级错误码，count 从 57→46
- 清理 i18n 资源：messages.properties 和 messages_zh_CN.properties 各移除 10 行已弃用条目
- 全量扫描确认：0 个对已移除枚举值的引用

## ydsz-common-domain 全维度优化建议
- 完整审查 43 个 Java 文件 + README + 配置元数据，输出 6 维度分析报告
- 关键发现:
  - **P0 架构**: src/test 目录完全空，0 个单元测试（README 自称基类库但无任何质量保障）
  - **P0 架构**: 死配置残留 6 个 (ydsz.domain.event.async-enabled / default-phase / ydsz.tree.lazy.*)
  - **P0 架构**: EventStore 接口公开但零实现，README 自承认"实现已删除"
  - **P0 架构**: FilterIgnoreConstant/TokenConstants/IdentityType/ServiceType/DataScopeType 越界到 domain 模块
  - **P0 架构**: 实体基类 5 层继承链（Persistable→BaseIdEntity→BaseAuditEntity→BaseEntity→BaseString/BaseLong），BaseString/BaseLong 几乎为空壳
  - **P1 性能**: SpEL 缓存用 Collections.synchronizedMap(LinkedHashMap) 高并发下是瓶颈，建议换 Caffeine
  - **P1 性能**: TreeBuilder.generatePaths 每次 O(n log n) 排序，可换 O(n) BFS
  - **P1 体验**: JobContextHolder 与 JobLoggerHolder 分两个 ThreadLocal，可合并 JobExecutionContext
  - **P2 过度设计**: @DomainService 仅是 @Component 语义化包装，收益小
  - **P2 过度设计**: 三个排序相关方法并存（addOrder/addAscOrder/addDescOrder/setOrderBy），API 不收敛
- 优势: 不可变 DomainEvent + 自动上下文填充 + 树构建 O(n) + 软删除/乐观锁拦截器解耦 + SQL 注入防护（白名单+正则）
- 详见输出报告

## ydsz-common-domain 过度设计评估（第二轮：可落地方案）
- 输出报告 docs/DOMAIN_MODULE_OVERDESIGN_AUDIT.md（按"内部精简、不替外部"原则）
- 实证数据:
  - 43 文件 / 5147 行；引用分布: PageQuery 系列 178 次为真核心，BaseEntity/* 业务模块 0 直接继承，Job/DAG 只 cronjob 用，EventStore 完全无人实现
  - 8 个注解中 7 个零引用（@DomainService/@SoftDelete/@TenantId/@CreatedBy/@CreateAt/@UpdatedBy/@UpdateAt），@Version 仅 1 处且与 README 第 458 行冲突
  - TreeBuilder 700 行但业务只 3 处用（Menu/Dept/Folder，节点<200）；PageQuery.sort 用 List<String> 字符串拼接 + 每次解析；DomainEvent 11 字段 + 12 setter 业务几乎不用
- 6 大病灶：
  1. 实体基类"双层寄生"（MpBaseEntity 间接依赖 BaseEntity，5 级继承）
  2. 7 个注解是装饰品（README 已自承认不合理但保留）
  3. DAG/Job 越界——应归 cronjob，不该在 common-domain
  4. TreeBuilder 复杂度超 ROI（缓存+DCL+LRU 对 200 节点菜单根本用不上）
  5. PageQuery 排序字段字符串化（应改 record OrderItem）
  6. DomainEvent 11 字段过度包装（应改 5 字段 + payload object）
- 落地路径 P0~P3 共 5~6 Sprint：
  - P0（本周）：删 8 注解/EventStore/TokenConstants/FilterIgnoreConstant/BaseDTO/跨层枚举迁移，~600 行死代码
  - P1（1~4 周）：PageQuery 用 List<OrderItem> / DAG+Job 迁 cronjob / 实体扁平化下沉 jdbc
  - P2（4~6 周）：TreeBuilder 砍 70% / DomainEvent 砍字段 / 状态机工厂升级
  - P3：README 重写 + 补单测 + 清配置元数据
- 收益预估：模块从 43 文件/5147 行 → ~15 文件/1700 行（-65%），业务模块不再被动引入 cronjob 设施

## ydsz-common-domain 全局引用分析（第三轮：模块集成度 + 能力利用率）
- 输出报告 docs/COMMON_DOMAIN_REFERENCE_AUDIT.md（576 行），与 overdesign 报告互补——本轮聚焦"其他模块用得怎么样"
- 模块×能力矩阵扫描结论：
  - 10 业务模块 import 排名：cronjob(30) > message(15) > userinfo(12) > nextwiki(9) = system(9) > workflow(5) > project(2) > literule(0) = agent(0) > gateway(无)
  - 子能力利用率：PageQuery/PageResult 6/10 模块（真核心），JobHandler 2/10（11 处实现），DAG 仅 cronjob，BaseStatusEnum 仅 2/10（20%），DomainEvent 仅 1/10，annotation 8 个注解业务模块 0~1 引用
- **🔴 P0 三大风险**（前两份报告未覆盖的盲点）：
  1. **隐式传递依赖**：ydsz-cronjob / ydsz-workflow / ydsz-message 三个模块 50 处 import + 11 个 implements JobHandler/BaseStatusEnum/DomainEvent，但 pom.xml 完全没声明 ydsz-common-domain 依赖——全靠 `ydsz-common-jdbc → ydsz-common-domain` 传递链。jdbc 一调整即连锁崩溃
  2. **死依赖**：ydsz-literule pom 显式声明 ydsz-common-domain 但代码 0 import，唯一关联是 RuleDefinitionDO 的 @Version（与 README 第 458 行设计原则冲突）
  3. **死注解 7 个**：@CreateAt/@CreatedBy/@UpdateAt/@UpdatedBy/@SoftDelete/@TenantId/@DomainService 业务模块使用次数全为 0
- **🟡 P1 能力未铺开**：
  - 状态枚举覆盖率仅 20%，userinfo/workflow/project/agent/cronjob/literule/system 都有状态机需求但都用 String 裸字段
  - DomainEvent 体系（ModuleEventTypes）注册表 11 个常量但只有 common-notify 1 处使用
  - PageResult 在 project/message 部分场景仍返回 IPage<T>，架构不一致
  - BaseDTO 覆盖率 2/10（仅 system+project）
  - TreeNode 强类型 API 业务模块 0 发挥，userinfo 的 Menu/Department 用的是 buildSimple 静态方式
  - ydsz-agent 是 10 个业务模块中唯一 0 import 的"集成空白"
  - MapProcessor/MapReduceProcessor 提供完整接口但 cronjob 0 实现
- 给出 90 天分阶段改造路线图 + 9 项关键 KPI 验收标准
- 文件：docs/COMMON_DOMAIN_REFERENCE_AUDIT.md（32KB）

## ydsz-common-domain 优化落地执行（第三轮）
- 按优先级逐项执行优化建议，过程中**检测到并行开发冲突**（外部进程持续重构 domain 模块）
- 已完成并生效（多数已被外部进程合入）:
  - P0-1: TypeEnum/IdentityType/ServiceType/DataScopeType 迁至 core.enums，TokenConstants/FilterIgnoreConstant 迁至 core.constant；21 个引用文件 import 已替换
  - P0-3: BaseString/BaseLong/@DomainService 已删除；外部进程更彻底移除整个 entity 目录
  - P0-5: additional-spring-configuration-metadata.json 死配置清理
  - P0-12: SpELConditionEvaluator 改 Caffeine（LRU+recordStats），DomainHealthIndicator 暴露 hitRate/hitCount/evictionCount
  - P1: TreeBuilder.generatePaths O(n) BFS、TreeNode.traverseBFS ArrayDeque、MapContext 容量 64（cronjob-domain 版）
  - P1-8: StateTransition/StateTransitions/StateTransitionTable 声明式状态机组件（3 新文件）
  - P1-9: JobHandler 增加 idempotentKey/maxAttempts/retryDelayMillis 契约
  - P1-25: JobContextHolder/JobLoggerHolder 合并为 JobExecutionContext（外部进程采用，放至 cronjob-domain）
  - P2-20: DomainException 体系 4 类 + BaseStatusEnum/StateTransitionTable/DomainEvent 接入
  - P1-19: PageQuery addOrders 批量方法 + setOrderBy @Deprecated
- **并行重构证据**: git log 持续新增 refactor(domain) 提交；entity/annotation/dto/dag 目录被移删；job 框架与 SpEL 移至 ydsz-cronjob-domain；TreeBuilder/StateTransitionTable 被外部精简
- **当前 HEAD 中间态（编译失败 2 处）**: ① domain/JobHandler 引用已移走的 ShardingContext ② common-jdbc/MpBaseEntity 等引用已删除的 domain.entity
- **待外部收敛后继续**: P0-4 单元测试、P2-22 Javadoc、P2-23 CHANGELOG、README 更新（含 EventStore 实为 OutboxEventStore 实现的修正）
- 教训: 本环境存在另一活跃开发会话，操作 domain 模块前应先确认无并行提交；rm 命令 safe-delete 在 Git Bash 下有路径 bug，删除文件一律用 git rm
