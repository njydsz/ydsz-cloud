# 2026-08-04 工作日志

## 第六轮：行业对标架构优化完善建议
- 完成 `docs/ARCHITECTURE_OPTIMIZATION_PROPOSAL.md`（46 条建议，P0-P3 分级 + 三阶段路线图）

## 第七轮：按优先级逐项实施优化
### 已落地（P0 全部 + P1 全部 + 部分 P2）
- **P0-1** CI/CD：`.github/workflows/backend-ci.yml`（质量门禁+镜像矩阵构建+Helm 部署）
- **P0-2** JWT 缓存击穿：`CachedJwtValidator` 接入 `CacheProtectionGuard.getWithProtection`
- **P0-3** 数据库脚本框架：`deploy/sql/`（schema/seed/verify + schema_check.sh）
- **P0-4** 连接池调优：`deploy/config/ydsz-common-datasource.yaml`（Druid 慢 SQL 监控+wall）
- **P1-1** CORS：`GatewayCorsConfig` + `CorsProperties`
- **P1-2** 限流分布式协调：`RateLimitFilter` 实例数自适应分摊 + fallback 指标
- **P1-3** API 版本：补充网关 `ApiVersionHeaderFilter`（X-API-Version）
- **P1-4** 链路追踪：`docker-compose.observability.yaml` + `TRACING_INTEGRATION_GUIDE.md`
- **P1-5** 监控：`prometheus-rules/ydsz-alerts.yml` + Grafana Dashboard + provisioning
- **P1-6** 慢 SQL：`V1.0.1__indexes_and_partition.sql`
- **P1-7** 压测：`load-test/`（K6 场景 + run.sh）
- **P1-8** 缓存：`CACHE_BEST_PRACTICES.md`
- **P1-9** 本地开发：`docker-compose.dev.yml` + `dev-start.sh`
- **P1-10** Runbook：`docs/runbooks/`（5 篇）
- **P2** 主动健康检查：`GrayLoadBalancerConfig`（5.0.2 需手动构造 HealthCheck，无 decorator 静态方法）
- **P2** Changelog：`.github/workflows/changelog.yml` + `cliff.toml`

### 修复历史遗留构建阻断（8 处）
1. `StringUtils.maskEmail` 引用已删 `isEmail()` → 内联正则
2. `BaseExceptionHandler.printStackTrace` 违反 Checkstyle → 手动堆栈遍历
3. common-cache 3 个空 while 块 → 加注释
4. common-jdbc 33+5 个类被误删 → `git checkout 984bd6814~1` 恢复（PageResponses 为新增）
5. `EventBuildException` 继承链 → 改继承 `AbstractYdszException`
6. `CoreHealthIndicator.java` 0 字节 → 重建
7. 9 个 gateway 文件 BOM → 移除
8. `ArchitectureRulesTest` ArchUnit 双参数 API → 链式调用

### 关键经验
- Spring Boot 4：`HttpHeaders.containsKey` 不可用，用 `containsHeader`
- spring-cloud-loadbalancer 5.0.2：HealthCheck 需 3 参构造器（delegate, factory, BiFunction探活函数）
- 本地 Maven：`/d/Maven/njydsz-maven`（3.9.16）；JDK：`C:\Program Files\Amazon Corretto\jdk21.0.8_9`
- 编译验证：`mvn compile -pl ydsz-gateway -am -DskipTests`（reactor 模式防旧 jar 误报）
- 项目 git 仓库根在 `D:\Code\ydsz`（ydsz-pmis 为子目录），部分路径相对 ../ 指向仓库外

## 第八轮：ydsz-frontend 21 项优化落地（本次）
按 v2 报告优先级逐项实现，覆盖 P0×6 + P1×9 + P2×6：

### 已完成（21/21 代码落地）
- **P0-2 adapter 去重**：9 个子应用的 vxe-table/form/component 适配器提取到 `@ydsz/shared-business/src/adapter/`，子应用改为 re-export（main 保留自研版因配置不同）
- **P0-3 字典缓存**：`comm/stores/src/modules/dict.ts` useDictStore（TTL 10min + loader 注入 + 并发去重 + 失效/清理），补 `dict.test.ts`；shared-business 增加 DictTag 组件
- **P0-4 Excel**：shared-business `use-excel-export.ts`（xlsx 写文件）/`use-excel-import.ts`（解析+校验）+ ExcelExportButton/ImportButton 组件
- **P0-5 测试基线**：9 个子应用各补 `src/__tests__/api-smoke.test.ts`（vi.mock requestClient 验证路径/方法）
- **P0-6 ESLint 升级**：新增 `configs/scoped-css.ts`（vue-scoped-css/enforce-style-type）与 `configs/i18n.ts`（vue-i18n/no-raw-text warn 渐进），注册进 defineConfig；catalog 补 2 个插件
- **P0-1 scoped 落地**：补 2 个存量文件 scoped（json-viewer、dingding-login 用 :deep）
- **P1-1 WebSocket**：shared-business `realtime/` RealtimeClient（心跳/指数退避重连/离线队列/频道订阅）+ useRealtime + 单测
- **P1-2 OpenAPI SDK**：修复 create-openapi-client 引用 bug（`./request-client`→`./request-setup`）；创建 system-web `api/sdk/schema.ts` 类型骨架 + sdk-client 改为真实 /api/v1 路径
- **P1-3 CRUD Hooks**：`use-server-pagination.ts` + `use-crud-table.ts`（分页/删除确认/弹窗/批量）
- **P1-4 i18n 示范**：dictType 页面迁移 $t() + 双语 business/page JSON 扩展
- **P1-5 审计日志**：`use-audit-log.ts` + `audit-log-table.vue`
- **P1-6 SWR 缓存**：`comm/effects/request/src/cache-adapter.ts` withSwrCache（stale-while-revalidate + LRU + 并发去重）+ 单测
- **P1-7 Bundle 监控**：`bash/check-bundle-size.mjs`（gzip 体积/各应用阈值/CI 接入）+ 根脚本
- **P1-8 状态组件**：EmptyState/ErrorState/AsyncState 三件套
- **P1-9 快捷键**：`use-keyboard-shortcut.ts`（scope/冲突检测/输入豁免）+ KeyboardHelp 面板
- **P2-1 iframe RPC**：iframe-sandbox 增加 rpc-call/rpc-result 消息类型 + callRpc/registerMainApi + 注入脚本 __MICRO_CALL_MAIN__/__MICRO_REGISTER_API__ + 单测
- **P2-2 流程设计器**：`use-flow-designer.ts`（Delete/Ctrl+C/V/缩放/居中快捷键）+ ApprovalTimeline 组件
- **P2-3 虚拟列表**：VirtualSelect（el-select-v2 封装）+ VirtualList（@tanstack/vue-virtual）
- **P2-4 操作引导**：`use-app-tour.ts`（自研轻量 tour：高亮/气泡/持久化）+ AppTour 组件
- **P2-5 移动端**：`use-responsive.ts`（isMobile/isTablet/列裁剪/栅格）
- **P2-6 Renovate**：`.github/renovate.json`（分组策略 + patch 自动合并 + 时区）

### 环境注意
- 安装依赖：`CI=true NODE_OPTIONS= COREPACK_ENABLE_STRICT=0 corepack pnpm install --no-frozen-lockfile --pm-on-fail=ignore`（safe-delete shim 会拦截临时目录删除，需清 NODE_OPTIONS）
- pnpm 命令路径：`C:/Users/Marvin/.workbuddy/binaries/node/versions/22.22.2/corepack.cmd`
