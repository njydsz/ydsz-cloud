<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.remisoft.literule.infra.mapper.RuleChainGraphMapper">

    <select id="selectByRuleCode" resultType="com.remisoft.literule.domain.entity.RuleChainGraphDO">
        SELECT id, rule_code, name, description, scenario, tenant_id, graph_version,
               status, content_json, created_by, created_at, updated_by, updated_at
        FROM remi_rule_chain_graph
        WHERE rule_code = #{ruleCode} LIMIT 1
    </select>

    <delete id="deleteByRuleCode">
        DELETE FROM remi_rule_chain_graph WHERE rule_code = #{ruleCode}
    </delete>

</mapper>
