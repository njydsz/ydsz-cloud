# =====================================================================
#  REMI · Message · DEV 环境配置（Nacos DataId）
#  DataId:    remi-message-dev.yaml
#  Group:     dev
#  端口:      9004
# =====================================================================

server:
  port: 9004
  servlet:
    context-path: /

spring:
  profiles: dev
  mail:
    host: ${MAIL_HOST:smtp.example.com}
    port: ${MAIL_PORT:465}
    username: ${MAIL_USER:no-reply@example.com}
    password: ${MAIL_PASSWORD:}
    default-encoding: UTF-8
    properties:
      mail:
        smtp:
          ssl:
            enable: true
          auth: true
          starttls:
            enable: true
          connectiontimeout: 10000
          timeout: 10000
          writetimeout: 10000

# REMI 消息引擎配置
remi:
  # JSON 引擎配置（替代原 spring.jackson.*，由 remi-common-json 读取）
  json:
    enabled: true
    date-format: yyyy-MM-dd HH:mm:ss
    naming-strategy: LOWER_CAMEL_CASE
    write-nulls: false
    monitoring-enabled: true
    safe-mode: true
    warmup-classes:
      - com.remisoft.common.feign.MessageRequest
      - com.remisoft.message.domain.entity.core.MsgLog
      - com.remisoft.message.domain.entity.config.MsgRouteRule
      - com.remisoft.message.domain.entity.config.MsgTemplate
      - com.remisoft.message.domain.entity.config.MsgCanary
  message:
    channel-enabled:
      SMS: true
      EMAIL: true
      PUSH: true
      INAPP: true
      WEBHOOK: true
      DINGTALK: true
      WECOM: true
      WECOM_APP: true
      FEISHU: true
    default-priority: NORMAL
    aggregate-scan-interval-ms: 60000
    retry-scan-interval-ms: 30000
    # P2-5: 消息 TTL（秒），超过此时间的定时消息自动丢弃；0 表示不检查
    message-ttl-seconds: 3600
    # P2-6: 全部已读分批大小，避免长事务；0 表示不分批
    mark-all-read-batch-size: 500
    global-daily-limit: 0
    global-hourly-limit: 0
    sms:
      provider: ${REMI_SMS_PROVIDER:mock}
      aliyun:
        access-key-id: ${REMI_SMS_ALIYUN_AK:}
        access-key-secret: ${REMI_SMS_ALIYUN_SK:}
        sign-name: ${REMI_SMS_ALIYUN_SIGN:}
        endpoint: dysmsapi.aliyuncs.com
        connect-timeout: 5000
        read-timeout: 10000
    push:
      provider: ${REMI_PUSH_PROVIDER:mock}
      getui:
        app-id: ${REMI_PUSH_GETUI_APPID:}
        app-key: ${REMI_PUSH_GETUI_APPKEY:}
        master-secret: ${REMI_PUSH_GETUI_MASTER:}
        base-url: https://restapi.getui.com
        connect-timeout: 5000
        read-timeout: 10000
  webhook:
    default-url: ${REMI_WEBHOOK_DEFAULT_URL:}
    connect-timeout: 5000
    read-timeout: 10000
  channel:
    dingtalk:
      default-token: ${REMI_DINGTALK_TOKEN:}
      secret: ${REMI_DINGTALK_SECRET:}
      connect-timeout: 5000
      read-timeout: 10000
    wechat-work:
      default-key: ${REMI_WECOM_KEY:}
      connect-timeout: 5000
      read-timeout: 10000
    feishu:
      default-hook: ${REMI_FEISHU_HOOK:}
      secret: ${REMI_FEISHU_SECRET:}
      connect-timeout: 5000
      read-timeout: 10000
    wecom-app:
      enabled: ${REMI_WECOM_APP_ENABLED:false}
      corp-id: ${REMI_WECOM_APP_CORPID:}
      corp-secret: ${REMI_WECOM_APP_CORPSECRET:}
      agent-id: ${REMI_WECOM_APP_AGENTID:0}
      base-url: https://qyapi.weixin.qq.com
      connect-timeout: 5000
      read-timeout: 10000
  # P0-1: 统一线程池配置（remi-common-thread）
  thread:
    enabled: true
    pools:
      messageBatch:
        core-size: 2
        max-size: 4
        queue-capacity: 200
        thread-name-prefix: msg-batch-
        reject-policy: CALLER_RUNS
        await-termination-seconds: 60
        keep-alive-seconds: 60
      # 通道级 Bulkhead 线程池
      msgEmail:
        core-size: 5
        max-size: 20
        queue-capacity: 100
        thread-name-prefix: msg-email-
        reject-policy: CALLER_RUNS
      msgSms:
        core-size: 10
        max-size: 30
        queue-capacity: 200
        thread-name-prefix: msg-sms-
        reject-policy: CALLER_RUNS
      msgDingtalk:
        core-size: 5
        max-size: 15
        queue-capacity: 100
        thread-name-prefix: msg-dingtalk-
        reject-policy: CALLER_RUNS
      msgWecom:
        core-size: 5
        max-size: 15
        queue-capacity: 100
        thread-name-prefix: msg-wecom-
        reject-policy: CALLER_RUNS
      msgWecomApp:
        core-size: 5
        max-size: 15
        queue-capacity: 100
        thread-name-prefix: msg-wecom-app-
        reject-policy: CALLER_RUNS
      msgFeishu:
        core-size: 5
        max-size: 15
        queue-capacity: 100
        thread-name-prefix: msg-feishu-
        reject-policy: CALLER_RUNS
      msgInapp:
        core-size: 3
        max-size: 10
        queue-capacity: 50
        thread-name-prefix: msg-inapp-
        reject-policy: CALLER_RUNS
      msgPush:
        core-size: 3
        max-size: 10
        queue-capacity: 50
        thread-name-prefix: msg-push-
        reject-policy: CALLER_RUNS
      msgWebhook:
        core-size: 5
        max-size: 15
        queue-capacity: 100
        thread-name-prefix: msg-webhook-
        reject-policy: CALLER_RUNS
rocketmq:
  name-server: ${ROCKETMQ_NAME_SERVER:127.0.0.1:9876}
  producer:
    group: ${ROCKETMQ_PRODUCER_GROUP:remi-message-producer-group}
    send-message-timeout: 5000
  consumer:
    enabled: ${ROCKETMQ_CONSUMER_ENABLED:true}

mybatis-plus:
  mapper-locations: classpath*:mapper/**/*.xml
  type-aliases-package: com.remisoft.message.entity
  configuration:
    map-underscore-to-camel-case: true
    cache-enabled: false
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl
  global-config:
    banner: false
    db-config:
      id-type: AUTO
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

# Resilience4j 限流
resilience4j:
  ratelimiter:
    instances:
      message-send:
        limit-for-period: 200
        limit-refresh-period: 1s
        timeout-duration: 0
      message-receipt:
        limit-for-period: 1000
        limit-refresh-period: 1s
        timeout-duration: 0

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    root: INFO
    "[com.remisoft]": DEBUG

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  metrics:
    tags:
      application: ${spring.application.name}
  prometheus:
    metrics:
      export:
        enabled: true
