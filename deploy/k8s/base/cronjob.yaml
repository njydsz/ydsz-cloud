apiVersion: apps/v1
kind: Deployment
metadata:
  name: ydsz-cronjob
  namespace: ydsz
  labels:
    app: ydsz-cronjob
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ydsz-cronjob
  template:
    metadata:
      labels:
        app: ydsz-cronjob
        tier: backend
    spec:
      # P0-7: 优雅停机宽限期，给 @PreDestroy drain 逻辑足够时间（默认 30s 不够）
      terminationGracePeriodSeconds: 60
      containers:
        - name: cronjob
          image: ydsz/cronjob:IMAGE_TAG
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 9005
          envFrom:
            - configMapRef:
                name: ydsz-common-config
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 1000m
              memory: 1Gi
          # P0-7: preStop hook 给 Spring @PreDestroy 足够时间排空在执行任务
          lifecycle:
            preStop:
              exec:
                command: ["sh", "-c", "sleep 15"]
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 9005
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 9005
            initialDelaySeconds: 60
            periodSeconds: 20
            failureThreshold: 5
---
apiVersion: v1
kind: Service
metadata:
  name: ydsz-cronjob
  namespace: ydsz
spec:
  type: ClusterIP
  selector:
    app: ydsz-cronjob
  ports:
  - name: http
    port: 9005
    targetPort: 9005
