apiVersion: apps/v1
kind: Deployment
metadata:
  name: ydsz-system
  namespace: pmis
  labels:
    app: ydsz-system
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ydsz-system
  template:
    metadata:
      labels:
        app: ydsz-system
        tier: backend
    spec:
      containers:
        - name: system
          image: ydsz/system:IMAGE_TAG
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 9002
          envFrom:
            - configMapRef:
                name: pmis-common-config
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 1000m
              memory: 1Gi
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 9002
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 9002
            initialDelaySeconds: 60
            periodSeconds: 20
            failureThreshold: 5
---
apiVersion: v1
kind: Service
metadata:
  name: ydsz-system
  namespace: pmis
spec:
  type: ClusterIP
  selector:
    app: ydsz-system
  ports:
  - name: http
    port: 9002
    targetPort: 9002
