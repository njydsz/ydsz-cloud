apiVersion: apps/v1
kind: Deployment
metadata:
  name: ydsz-pmis-message
  namespace: pmis
  labels:
    app: ydsz-pmis-message
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ydsz-pmis-message
  template:
    metadata:
      labels:
        app: ydsz-pmis-message
        tier: backend
    spec:
      containers:
        - name: message
          image: ydsz-pmis/message:IMAGE_TAG
          imagePullPolicy: IfNotPresent
          securityContext:
            runAsNonRoot: true
            runAsUser: 65532
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          ports:
            - name: http
              containerPort: 9004
          envFrom:
            - configMapRef:
                name: pmis-common-config
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 1000m
              memory: 1Gi
          startupProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 9004
            failureThreshold: 30
            periodSeconds: 10
            timeoutSeconds: 3
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 9004
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 9004
            initialDelaySeconds: 60
            periodSeconds: 20
            failureThreshold: 5
          volumeMounts:
            - name: tmp-volume
              mountPath: /tmp
      volumes:
        - name: tmp-volume
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: ydsz-pmis-message
  namespace: pmis
spec:
  type: ClusterIP
  selector:
    app: ydsz-pmis-message
  ports:
  - name: http
    port: 9004
    targetPort: 9004
