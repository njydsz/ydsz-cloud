# =============================================================================
#  YDSZ PMIS · 开发/测试环境变量模板
# -----------------------------------------------------------------------------
#  使用方法:
#    1. 复制本文件为 .env：    cp deploy/.env.example deploy/.env
#    2. 按需修改账号密码（生产必须改）
#    3. docker compose 会自动读取 deploy/.env
#    4. 后端服务启动时也会读取此文件（通过 set -a; source deploy/.env; set +a）
# =============================================================================

# -----------------------------------------------------------------------------
#  PostgreSQL 主数据库
# -----------------------------------------------------------------------------
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5432
POSTGRES_DB=ydsz_pmis
POSTGRES_USER=pmis
POSTGRES_PASSWORD=pmis123

# 后端使用（与 docker 容器内一致）
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=ydsz_pmis
DB_USER=pmis
DB_PASSWORD=pmis123

# -----------------------------------------------------------------------------
#  Redis
# -----------------------------------------------------------------------------
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=pmis123
REDIS_DB=0

# -----------------------------------------------------------------------------
#  Nacos 服务注册 + 配置中心
# -----------------------------------------------------------------------------
NACOS_SERVER_ADDR=127.0.0.1:8848
NACOS_NAMESPACE=pmis
NACOS_USERNAME=nacos
NACOS_PASSWORD=nacos
# profile：dev / sit / uat / prod
NACOS_PROFILE=dev

# -----------------------------------------------------------------------------
#  Sentinel 限流 Dashboard（可选，规则也可放 Nacos）
# -----------------------------------------------------------------------------
SENTINEL_DASHBOARD=127.0.0.1:8858
SENTINEL_TRANSPORT_PORT=8719

# -----------------------------------------------------------------------------
#  MinIO 对象存储
# -----------------------------------------------------------------------------
MINIO_ENDPOINT=http://127.0.0.1:9100
MINIO_API_PORT=9100
MINIO_CONSOLE_PORT=9101
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=pmis

# -----------------------------------------------------------------------------
#  XXL-Job 调度（可选，未启动 XXL-Job Admin 时保留默认值即可）
# -----------------------------------------------------------------------------
XXL_JOB_ADMIN_ADDRESSES=http://127.0.0.1:9100/xxl-job-admin
XXL_JOB_USERNAME=admin
XXL_JOB_PASSWORD=admin123
XXL_JOB_ACCESS_TOKEN=default-token
XXL_JOB_APPNAME=pmis-executor
XXL_JOB_EXECUTOR_PORT=9999

# -----------------------------------------------------------------------------
#  RocketMQ（可选，未启动 NameServer 时保留默认值即可）
# -----------------------------------------------------------------------------
ROCKETMQ_NAME_SERVER=127.0.0.1:9876
ROCKETMQ_PRODUCER_GROUP=pmis-producer-group
ROCKETMQ_CONSUMER_ENABLED=true

# -----------------------------------------------------------------------------
#  Seata 分布式事务（可选，未启动 Seata Server 时关闭）
# -----------------------------------------------------------------------------
SEATA_ENABLED=false
SEATA_SERVER_ADDR=127.0.0.1:8091

# -----------------------------------------------------------------------------
#  Elasticsearch（可选，全文搜索已改用 PG tsvector）
# -----------------------------------------------------------------------------
ES_URIS=http://127.0.0.1:9200

# -----------------------------------------------------------------------------
#  SMTP 邮件（内网部署如不发邮件，留空即可）
# -----------------------------------------------------------------------------
MAIL_HOST=smtp.example.com
MAIL_PORT=465
MAIL_USER=no-reply@example.com
MAIL_PASSWORD=

# -----------------------------------------------------------------------------
#  Webhook 默认地址（企微/钉钉/飞书机器人，留空表示不启用兜底）
# -----------------------------------------------------------------------------
PMIS_WEBHOOK_DEFAULT_URL=

# -----------------------------------------------------------------------------
#  JWT 签名密钥（生产环境 ≥ 32 字节，从 Vault 注入）
# -----------------------------------------------------------------------------
JWT_SECRET=please-replace-with-your-32-bytes-random-secret-key-here
# 登录是否强制图形验证码
CAPTCHA_REQUIRED=false

# -----------------------------------------------------------------------------
#  AI Agent LLM Provider
# -----------------------------------------------------------------------------
# Provider 选择: mock(默认) | spring-ai-openai | dashscope | qianfan
LLM_PROVIDER=mock
LLM_TIMEOUT_MILLIS=10000
LLM_MAX_RETRIES=2
LLM_FALLBACK_TO_MOCK=true
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com
OPENAI_MODEL=gpt-4o-mini

# -----------------------------------------------------------------------------
#  日志级别（root: INFO / com.njydsz.pmis: DEBUG）
# -----------------------------------------------------------------------------
LOG_ROOT_LEVEL=INFO
LOG_PMIS_LEVEL=DEBUG
