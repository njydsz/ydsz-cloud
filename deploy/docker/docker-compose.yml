# ====================================================================
# 南京云顶 PMIS 总体编排（聚合 base + apps）
# 包含：基础环境（base.yml）+ 14 个微服务 + 周边组件（apps.yml）
# 用法：docker compose up -d
# 拆分：开发用 base / 生产用 apps 单独部署
# ====================================================================

name: pmis

include:
  - path: ./docker-compose.base.yml
  - path: ./docker-compose.apps.yml
