<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <artifactId>remi-common-base</artifactId>
    <name>remi-common-base</name>
    <description>remi-common-base 是 Web/App 公共 HTTP 基座模块，承载 CORS、时区、安全头、日志拦截、上下文清理、API 文档(OpenAPI/Knife4j)、文档导出等共享逻辑</description>

    <parent>
        <groupId>com.remisoft</groupId>
        <artifactId>remi-common</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <dependencies>
        <!-- Spring Web 支持 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- 公共核心模块 -->
        <dependency>
            <groupId>com.remisoft</groupId>
            <artifactId>remi-common-core</artifactId>
        </dependency>

        <!-- 公共工具模块 -->
        <dependency>
            <groupId>com.remisoft</groupId>
            <artifactId>remi-common-util</artifactId>
        </dependency>

        <!-- 公共异常模块 -->
        <dependency>
            <groupId>com.remisoft</groupId>
            <artifactId>remi-common-exception</artifactId>
        </dependency>

        <!-- 公共 JSON 模块（直接使用 RemiJson，显式声明） -->
        <dependency>
            <groupId>com.remisoft</groupId>
            <artifactId>remi-common-json</artifactId>
        </dependency>

        <!-- Micrometer Core（AbstractModuleMetrics 基类需要） -->
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-core</artifactId>
        </dependency>

        <!-- Spring Boot Actuator（可选，用于 HealthIndicator） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-actuator</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- spring-boot-health (Spring Boot 4.x: health classes moved from actuator to health module) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-health</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- springdoc-openapi（可选，业务项目可选择是否使用 OpenAPI 文档） -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-api</artifactId>
            <optional>true</optional>
            <exclusions>
                <!-- 排除 swagger-core-jakarta 传递的 snakeyaml，统一使用 Spring Boot BOM 管理的 2.4 版本 -->
                <exclusion>
                    <groupId>org.yaml</groupId>
                    <artifactId>snakeyaml</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <!-- Knife4j 提供 UI 增强（可选） -->
        <dependency>
            <groupId>com.github.xiaoymin</groupId>
            <artifactId>knife4j-openapi3-spring-boot-starter</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- WebFlux 支持（可选） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webflux</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Lombok 简化代码 -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- Spring Boot 配置处理器 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- P0-1: 单元测试基础设施（JUnit 5 + Mockito + AssertJ） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
