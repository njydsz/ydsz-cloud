{
  "groups": [
    {
      "name": "remi.core",
      "description": "REMI Core 模块配置组，包含分页、MDC 过滤器、核心开关配置。",
      "type": "com.remisoft.common.core.config.CoreProperties"
    }
  ],
  "properties": [
    {
      "name": "remi.core.enabled",
      "type": "java.lang.Boolean",
      "description": "是否启用 Core 模块自动配置（i18n 解析器、分页配置同步、Micrometer 指标注册）。禁用后所有运行时注入回退到编译期默认值。",
      "defaultValue": true
    },
    {
      "name": "remi.core.max-page-size",
      "type": "java.lang.Integer",
      "description": "运行时最大每页记录数上限（1-5000），防止一次性拉取过多数据导致内存或性能问题。须满足 max-page-size >= default-page-size（JSR-303 交叉校验）。",
      "defaultValue": 1000
    },
    {
      "name": "remi.core.default-page-size",
      "type": "java.lang.Integer",
      "description": "运行时默认每页记录数（>=1），分页查询未指定 pageSize 时使用。必须小于等于 max-page-size。",
      "defaultValue": 20
    },
    {
      "name": "remi.core.tenant-mdc-filter-order",
      "type": "java.lang.Integer",
      "description": "租户 MDC 过滤器注册顺序（FilterRegistrationBean order 值），默认为 HIGHEST_PRECEDENCE + 100，确保租户信息在业务过滤器之前写入 MDC。",
      "defaultValue": -2147483647
    }
  ]
}
