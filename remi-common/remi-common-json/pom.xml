<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.remisoft</groupId>
        <artifactId>remi-common</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>remi-common-json</artifactId>
    <packaging>jar</packaging>
    <name>remi-common-json</name>
    <description>REMI 公共依赖 - 高性能 JSON 序列化/反序列化引擎（RemiJson，零外部 JSON 库依赖）</description>

    <properties>
        <asm.version>9.9</asm.version>
        <jmh.version>1.37</jmh.version>
    </properties>

    <dependencies>
        <!-- Lombok（父 POM 已管理版本和 annotation processor） -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
        </dependency>

        <!-- SLF4J（可选） -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- ASM 字节码优化（可选） -->
        <dependency>
            <groupId>org.ow2.asm</groupId>
            <artifactId>asm</artifactId>
            <version>${asm.version}</version>
            <optional>true</optional>
        </dependency>

        <!-- Spring Boot AutoConfigure -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-autoconfigure</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- Spring Boot Configuration Processor -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Spring Web（HTTP 消息转换器集成） -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-web</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Reactive Streams API（WebFlux 响应式场景 JsonReactiveEncoder 使用，版本由 Spring Boot BOM 管理） -->
        <dependency>
            <groupId>org.reactivestreams</groupId>
            <artifactId>reactive-streams</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Spring Boot Actuator（健康检查，optional） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-actuator</artifactId>
            <optional>true</optional>
        </dependency>
        <!-- Spring Boot 4.x: health classes moved to health module -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-health</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Micrometer（指标监控，optional） -->
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-core</artifactId>
            <optional>true</optional>
        </dependency>

        <!--
            Jackson annotations（optional，编译期可见，运行期不强制）
            spring-boot 4.x 的 spring-boot-health / spring-boot-actuator / spring-web 7.0.x
            的 class 文件字节码中带有 @JsonInclude 等 Jackson 注解。本模块刻意不强制依赖
            Jackson 引擎（用 Remi RemiJson 替代），但需要 Jackson annotations jar 提供
            注解 class 文件供 javac 在读取这些依赖 class 时解析，否则会持续报
            "未知的枚举常量 com.fasterxml.jackson.annotation.JsonInclude.Include.NON_EMPTY"
            警告。声明为 optional 即可在编译期消除警告，又不强制本模块使用者必须引入 Jackson 核心。
        -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-annotations</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Jakarta Annotation -->
        <dependency>
            <groupId>jakarta.annotation</groupId>
            <artifactId>jakarta.annotation-api</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- Jakarta Validation（可选，用于 @Validated 配置校验） -->
        <dependency>
            <groupId>jakarta.validation</groupId>
            <artifactId>jakarta.validation-api</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- JMH Benchmark -->
        <dependency>
            <groupId>org.openjdk.jmh</groupId>
            <artifactId>jmh-core</artifactId>
            <version>${jmh.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.openjdk.jmh</groupId>
            <artifactId>jmh-generator-annprocess</artifactId>
            <version>${jmh.version}</version>
            <scope>test</scope>
        </dependency>

        <!-- JMH Benchmark 对比基库（test scope，不与生产代码绑定） -->
        <!--
            注：Gson / Hutool-JSON / org.json / json-simple 历史上禁止直接声明（统一 JSON 能力收敛到 RemiJson），
            竞品对比基准测试仅保留 Jackson（optional）和 Fastjson2（独立 groupId 不受限）。
        -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.datatype</groupId>
            <artifactId>jackson-datatype-jsr310</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.alibaba.fastjson2</groupId>
            <artifactId>fastjson2</artifactId>
            <version>2.0.57</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <!--
        GraalVM Native Image 试点（P2） - 仅 development 阶段显式激活：
        mvn native:compile -Pnative
        详见 deploy/docs/GRAALVM_NATIVE_IMAGE_TRIAL.md
    -->
    <profiles>
        <profile>
            <id>native</id>
            <properties>
                <native-maven-plugin.version>0.10.6</native-maven-plugin.version>
            </properties>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.graalvm.buildtools</groupId>
                        <artifactId>native-maven-plugin</artifactId>
                        <version>${native-maven-plugin.version}</version>
                        <extensions>true</extensions>
                        <executions>
                            <execution>
                                <id>test-native</id>
                                <goals>
                                    <goal>test</goal>
                                </goals>
                                <phase>test</phase>
                            </execution>
                            <execution>
                                <id>build-native</id>
                                <goals>
                                    <goal>compile-no-fork</goal>
                                </goals>
                                <phase>package</phase>
                            </execution>
                        </executions>
                        <configuration>
                            <imageName>remi-common-json</imageName>
                            <mainClass>com.remisoft.common.json.RemiJson</mainClass>
                            <buildArgs>
                                <buildArg>--no-fallback</buildArg>
                                <buildArg>--verbose</buildArg>
                                <buildArg>-H:+ReportExceptionStackTraces</buildArg>
                                <buildArg>-H:ReflectionConfigurationFiles=${project.basedir}/src/main/resources/META-INF/native-image/com.remisoft/remi-common-json/reflect-config.json</buildArg>
                                <buildArg>-H:ResourceConfigurationFiles=${project.basedir}/src/main/resources/META-INF/native-image/com.remisoft/remi-common-json/resource-config.json</buildArg>
                                <buildArg>-H:+InlineBeforeAnalysis</buildArg>
                            </buildArgs>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

</project>
