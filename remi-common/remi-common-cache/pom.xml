<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.remisoft</groupId>
    <artifactId>remi-common</artifactId>
    <version>1.0.0-SNAPSHOT</version>
  </parent>
  <artifactId>remi-common-cache</artifactId>
  <name>remi-common-cache</name>
  <description>REMI Common Cache - 多策略本地缓存框架（零第三方依赖核心，可选 Spring/Micrometer 集成）</description>

  <properties>
    <jmh.version>1.37</jmh.version>
    <!-- JaCoCo 已全局禁用，提供默认空值避免 @{argLine} 占位符未解析 -->
    <argLine></argLine>
  </properties>

  <dependencies>
    <!--common-util（传递引入 common-json、SLF4J 等）-->
    <dependency>
      <groupId>com.remisoft</groupId>
      <artifactId>remi-common-util</artifactId>
    </dependency>
    <!--common-json（统一 JSON 引擎，广播消息序列化）-->
    <dependency>
      <groupId>com.remisoft</groupId>
      <artifactId>remi-common-json</artifactId>
    </dependency>
    <!--common-redis（多级缓存 L2 后端，可选）-->
    <dependency>
      <groupId>com.remisoft</groupId>
      <artifactId>remi-common-redis</artifactId>
      <optional>true</optional>
    </dependency>
    <!--common-exception-->
    <dependency>
      <groupId>com.remisoft</groupId>
      <artifactId>remi-common-exception</artifactId>
    </dependency>
    <!-- Spring Context（Cache 接口） -->
    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-context</artifactId>
      <scope>provided</scope>
    </dependency>
    <!-- Spring AOP（@Cached 注解切面） -->
    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-aop</artifactId>
      <scope>provided</scope>
      <optional>true</optional>
    </dependency>
    <!-- AspectJ Weaver（@Aspect / @Around 注解支持） -->
    <dependency>
      <groupId>org.aspectj</groupId>
      <artifactId>aspectjweaver</artifactId>
      <scope>provided</scope>
      <optional>true</optional>
    </dependency>
    <!-- Spring Boot Autoconfigure -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-autoconfigure</artifactId>
      <scope>provided</scope>
    </dependency>
    <!-- Spring Boot Configuration Processor -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-configuration-processor</artifactId>
      <optional>true</optional>
    </dependency>
    <!-- Micrometer 可观测性（可选） -->
    <dependency>
      <groupId>io.micrometer</groupId>
      <artifactId>micrometer-core</artifactId>
      <scope>provided</scope>
      <optional>true</optional>
    </dependency>
    <!--
        Jackson annotations（provided，编译期可见，运行期不强制）
        spring-boot 4.x 的 spring-boot-health / spring-boot-actuator / spring-web 7.0.x
        的 class 文件字节码中带有 @JsonInclude 等 Jackson 注解。声明为 provided 即可在
        编译期消除 "未知的枚举常量 com.fasterxml.jackson.annotation.JsonInclude.Include.NON_EMPTY"
        警告，又不强制本模块使用者必须引入 Jackson 核心。
    -->
    <dependency>
      <groupId>com.fasterxml.jackson.core</groupId>
      <artifactId>jackson-annotations</artifactId>
      <scope>provided</scope>
      <optional>true</optional>
    </dependency>
    <!-- Spring Boot Health（HealthIndicator，Spring Boot 4.x 独立模块） -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-health</artifactId>
      <scope>provided</scope>
      <optional>true</optional>
    </dependency>
    <!-- Resilience4j 降级/熔断（可选） -->
    <dependency>
      <groupId>io.github.resilience4j</groupId>
      <artifactId>resilience4j-circuitbreaker</artifactId>
      <scope>provided</scope>
      <optional>true</optional>
    </dependency>
    <!-- SkyWalking Trace（可选） -->
    <dependency>
      <groupId>org.apache.skywalking</groupId>
      <artifactId>apm-toolkit-trace</artifactId>
      <scope>provided</scope>
      <optional>true</optional>
    </dependency>
    <!--lombok-->
    <dependency>
      <groupId>org.projectlombok</groupId>
      <artifactId>lombok</artifactId>
      <scope>provided</scope>
    </dependency>
    <!-- Test -->
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.assertj</groupId>
      <artifactId>assertj-core</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.awaitility</groupId>
      <artifactId>awaitility</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- JMH Benchmark（test scope，避免打包进生产 jar） -->
    <dependency>
      <groupId>org.openjdk.jmh</groupId>
      <artifactId>jmh-core</artifactId>
      <version>${jmh.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.openjdk.jmh</groupId>
      <artifactId>jmh-generator-annprocess</artifactId>
      <version>${jmh.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

</project>
