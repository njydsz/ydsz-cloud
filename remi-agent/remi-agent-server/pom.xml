<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.remisoft</groupId>
        <artifactId>remi-agent</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>
    <artifactId>remi-agent-server</artifactId>
    <name>remi-agent-server</name>
    <description>Application Layer - Agent 执行引擎、对话服务、工具服务、RAG 服务</description>
    <dependencies>
        <dependency>
            <groupId>com.remisoft</groupId>
            <artifactId>remi-agent-infra</artifactId>
        </dependency>
        <dependency>
            <groupId>com.remisoft</groupId>
            <artifactId>remi-common-jdbc</artifactId>
        </dependency>
        <dependency>
            <groupId>com.remisoft</groupId>
            <artifactId>remi-common-feign</artifactId>
        </dependency>
        <dependency>
            <groupId>com.remisoft</groupId>
            <artifactId>remi-common-web</artifactId>
        </dependency>
        <!-- remi-common-core: explicit declaration for direct usage (was transitive via common-web) -->
        <dependency>
            <groupId>com.remisoft</groupId>
            <artifactId>remi-common-core</artifactId>
        </dependency>
    
        <dependency>
            <groupId>com.remisoft</groupId>
            <artifactId>remi-common-cache</artifactId>
        </dependency>
        <dependency>
            <groupId>com.remisoft</groupId>
            <artifactId>remi-common-lock</artifactId>
        </dependency>
        <!-- P0-2: 事务性 Outbox 事件模式 -->
        <dependency>
            <groupId>com.remisoft</groupId>
            <artifactId>remi-common-event</artifactId>
        </dependency>
        <!-- P0-1: 统一消息队列抽象（异步任务、工具调用结果回传、审批请求） -->
        <dependency>
            <groupId>com.remisoft</groupId>
            <artifactId>remi-common-queue</artifactId>
        </dependency>
        <dependency>
            <groupId>com.remisoft</groupId>
            <artifactId>remi-common-thread</artifactId>
        </dependency>
        <dependency>
            <groupId>com.remisoft</groupId>
            <artifactId>remi-common-docs</artifactId>
        </dependency>
        <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>dynamic-datasource-spring-boot3-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <!-- P2-2: 单元测试依赖（JUnit Jupiter + Mockito + AssertJ + Spring Test） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
            <dependency>
            <groupId>com.remisoft</groupId>
            <artifactId>remi-common-json</artifactId>
        </dependency>
        <!-- P0-1: 统一监控告警（Sentry SPI：ELK/Loki 日志推送 + 告警收敛 + SLA） -->
        <dependency>
            <groupId>com.remisoft</groupId>
            <artifactId>remi-common-sentry</artifactId>
        </dependency>
        <!-- P2-3: 统一文件存储接入 -->
        <dependency>
            <groupId>com.remisoft</groupId>
            <artifactId>remi-common-file</artifactId>
        </dependency>
        <!-- P0-3: 跨模块 Feign 客户端（Agent 调用项目服务、用户信息查询） -->
        <dependency>
            <groupId>com.remisoft</groupId>
            <artifactId>remi-project-api</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.remisoft</groupId>
            <artifactId>remi-userinfo-api</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- P1-2: 通知渠道（Agent 执行结果通知） -->
        <dependency>
            <groupId>com.remisoft</groupId>
            <artifactId>remi-common-notify</artifactId>
        </dependency>
        <!-- P1-2: 搜索 Provider SPI（知识库搜索） -->
        <dependency>
            <groupId>com.remisoft</groupId>
            <artifactId>remi-common-search</artifactId>
        </dependency>
        <!-- P0-3: 多租户隔离 -->
        <dependency>
            <groupId>com.remisoft</groupId>
            <artifactId>remi-common-tenant</artifactId>
        </dependency>
        <!-- P2-5: 统一 Excel 导入导出 -->
        <dependency>
            <groupId>com.remisoft</groupId>
            <artifactId>remi-common-excel</artifactId>
        </dependency>
    </dependencies>
</project>
