<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.njydsz</groupId>
        <artifactId>ydsz-nextwiki</artifactId>
        <version>26.09.01</version>
    </parent>
    <artifactId>ydsz-nextwiki-api</artifactId>
    <name>ydsz-nextwiki-api</name>
    <description>API Layer - Feign 契约层（FeignClient + Fallback），对外暴露网盘知识库的跨模块 RPC 能力</description>
    <dependencies>
        <!-- DDD: api 依赖 domain 获取 DTO/Query/VO -->
        <dependency>
            <groupId>com.njydsz</groupId>
            <artifactId>ydsz-nextwiki-domain</artifactId>
        </dependency>
        <!-- 统一响应 -->
        <dependency>
            <groupId>com.njydsz</groupId>
            <artifactId>ydsz-common-core</artifactId>
        </dependency>
        <!-- FeignClient 常量 -->
        <dependency>
            <groupId>com.njydsz</groupId>
            <artifactId>ydsz-common-feign</artifactId>
        </dependency>
        <!-- Jakarta Validation -->
        <dependency>
            <groupId>jakarta.validation</groupId>
            <artifactId>jakarta.validation-api</artifactId>
        </dependency>
        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
    </dependencies>
</project>
