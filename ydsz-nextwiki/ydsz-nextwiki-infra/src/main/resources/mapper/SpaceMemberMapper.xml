<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.njydsz.nextwiki.infra.mapper.SpaceMemberMapper">

  <resultMap id="BaseResultMap" type="com.njydsz.nextwiki.infra.entity.SpaceMember">
    <id column="id" property="id"/>
    <result column="space_id" property="spaceId"/>
    <result column="user_id" property="userId"/>
    <result column="role" property="role"/>
    <result column="tenant_id" property="tenantId"/>
    <result column="joined_at" property="joinedAt"/>
    <result column="created_by" property="createdBy"/>
    <result column="updated_at" property="updatedAt"/>
    <result column="updated_by" property="updatedBy"/>
    <result column="deleted" property="deleted"/>
  </resultMap>

  <sql id="Base_Column_List">
    id, space_id, user_id, role, tenant_id, joined_at, created_by, updated_at, updated_by, deleted
  </sql>

  <!-- 查询空间的成员列表（按角色优先级排序，未删除） -->
  <select id="selectBySpaceId" resultMap="BaseResultMap">
    SELECT <include refid="Base_Column_List"/>
    FROM ydsz_space_member
    WHERE space_id = #{spaceId} AND deleted = FALSE
    ORDER BY
      CASE role
        WHEN 'owner' THEN 1
        WHEN 'admin' THEN 2
        WHEN 'editor' THEN 3
        WHEN 'viewer' THEN 4
        ELSE 5
      END,
      joined_at ASC
  </select>

  <!-- 查询用户参与的空间列表 -->
  <select id="selectByUserId" resultMap="BaseResultMap">
    SELECT <include refid="Base_Column_List"/>
    FROM ydsz_space_member
    WHERE user_id = #{userId} AND deleted = FALSE
    ORDER BY joined_at DESC
  </select>

  <!-- 根据空间ID和用户ID查找成员 -->
  <select id="selectBySpaceIdAndUserId" resultMap="BaseResultMap">
    SELECT <include refid="Base_Column_List"/>
    FROM ydsz_space_member
    WHERE space_id = #{spaceId} AND user_id = #{userId} AND deleted = FALSE
    LIMIT 1
  </select>

  <!-- 统计空间成员数量 -->
  <select id="countBySpaceId" resultType="int">
    SELECT COUNT(*)
    FROM ydsz_space_member
    WHERE space_id = #{spaceId} AND deleted = FALSE
  </select>

  <!-- 检查用户是否在空间中 -->
  <select id="existsBySpaceIdAndUserId" resultType="boolean">
    SELECT COUNT(*) > 0
    FROM ydsz_space_member
    WHERE space_id = #{spaceId} AND user_id = #{userId} AND deleted = FALSE
  </select>

  <!-- 更新成员角色 -->
  <update id="updateRole">
    UPDATE ydsz_space_member
    SET role = #{role}, updated_at = NOW(), updated_by = #{userId}
    WHERE space_id = #{spaceId} AND user_id = #{userId} AND deleted = FALSE
  </update>

  <!-- 删除成员 -->
  <delete id="deleteBySpaceIdAndUserId">
    DELETE FROM ydsz_space_member
    WHERE space_id = #{spaceId} AND user_id = #{userId}
  </delete>

</mapper>
