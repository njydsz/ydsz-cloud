<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.njydsz.nextwiki.infra.mapper.FileVersionMapper">

    <resultMap id="FileVersionResult" type="com.njydsz.nextwiki.domain.entity.FileVersion">
        <id column="id" property="id"/>
        <result column="file_node_id" property="fileNodeId"/>
        <result column="version_number" property="versionNumber"/>
        <result column="storage_key" property="storageKey"/>
        <result column="size" property="size"/>
        <result column="file_hash" property="fileHash"/>
        <result column="mime_type" property="mimeType"/>
        <result column="remark" property="remark"/>
        <result column="change_type" property="changeType"/>
        <result column="is_active" property="isActive"/>
        <result column="status" property="status"/>
        <result column="created_by" property="createdBy"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_by" property="updatedBy"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="revision" property="revision"/>
        <result column="is_deleted" property="isDeleted"/>
    </resultMap>

    <select id="selectByFileNodeId" resultMap="FileVersionResult">
        SELECT id, file_node_id, version_number, storage_key, size, file_hash, mime_type, remark, change_type, is_active, status, created_by, created_at, updated_by, updated_at, revision, deleted, tenant_id
        FROM ydsz_wiki_file_version
        WHERE file_node_id = #{fileNodeId} AND is_deleted = 0
        ORDER BY version_number DESC
    </select>

    <select id="selectByVersion" resultMap="FileVersionResult">
        SELECT id, file_node_id, version_number, storage_key, size, file_hash, mime_type, remark, change_type, is_active, status, created_by, created_at, updated_by, updated_at, revision, deleted, tenant_id
        FROM ydsz_wiki_file_version
        WHERE file_node_id = #{fileNodeId} AND version_number = #{versionNumber} AND is_deleted = 0
    </select>

    <select id="selectActiveVersion" resultMap="FileVersionResult">
        SELECT id, file_node_id, version_number, storage_key, size, file_hash, mime_type, remark, change_type, is_active, status, created_by, created_at, updated_by, updated_at, revision, deleted, tenant_id
        FROM ydsz_wiki_file_version
        WHERE file_node_id = #{fileNodeId} AND is_active = TRUE AND is_deleted = 0
        LIMIT 1
    </select>

    <select id="countByFileNodeId" resultType="int">
        SELECT COUNT(*) FROM ydsz_wiki_file_version
        WHERE file_node_id = #{fileNodeId} AND is_deleted = 0
    </select>

    <select id="selectOldestVersions" resultMap="FileVersionResult">
        SELECT id, file_node_id, version_number, storage_key, size, file_hash, mime_type, remark, change_type, is_active, status, created_by, created_at, updated_by, updated_at, revision, deleted, tenant_id
        FROM ydsz_wiki_file_version
        WHERE file_node_id = #{fileNodeId} AND is_deleted = 0
        ORDER BY version_number ASC
        LIMIT #{limit}
    </select>

    <update id="updateWithRevision">
        UPDATE ydsz_wiki_file_version
        SET
            file_node_id = #{version.fileNodeId},
            version_number = #{version.versionNumber},
            storage_key = #{version.storageKey},
            size = #{version.size},
            file_hash = #{version.fileHash},
            mime_type = #{version.mimeType},
            remark = #{version.remark},
            change_type = #{version.changeType},
            is_active = #{version.active},
            status = #{version.status},
            updated_by = #{version.updatedBy},
            updated_at = #{version.updatedAt},
            revision = revision + 1
        WHERE id = #{version.id}
          AND is_deleted = 0
          AND revision = #{version.revision}
    </update>

</mapper>
