<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.njydsz.nextwiki.infra.mapper.UserRecentMapper">

  <resultMap id="BaseResultMap" type="com.njydsz.nextwiki.infra.entity.UserRecent">
    <id column="id" property="id"/>
    <result column="user_id" property="userId"/>
    <result column="node_id" property="nodeId"/>
    <result column="tenant_id" property="tenantId"/>
    <result column="access_type" property="accessType"/>
    <result column="accessed_at" property="accessedAt"/>
    <result column="created_at" property="createdAt"/>
    <result column="updated_at" property="updatedAt"/>
    <result column="deleted" property="deleted"/>
  </resultMap>

  <sql id="Base_Column_List">
    id, user_id, node_id, tenant_id, access_type, accessed_at, created_at, updated_at, deleted
  </sql>

  <!-- 查询用户最近访问列表（按访问时间倒序，未删除） -->
  <select id="selectByUserIdOrderByAccessedAt" resultMap="BaseResultMap">
    SELECT <include refid="Base_Column_List"/>
    FROM ydsz_nw_user_recent
    WHERE user_id = #{userId} AND tenant_id = #{tenantId} AND deleted = FALSE
    ORDER BY accessed_at DESC
    LIMIT #{limit}
  </select>

  <!-- 分页查询用户最近访问列表 -->
  <select id="selectByUserIdWithPage" resultMap="BaseResultMap">
    SELECT <include refid="Base_Column_List"/>
    FROM ydsz_nw_user_recent
    WHERE user_id = #{userId} AND tenant_id = #{tenantId} AND deleted = FALSE
    ORDER BY accessed_at DESC
    LIMIT #{limit} OFFSET #{offset}
  </select>

  <!-- 统计用户最近访问记录数量 -->
  <select id="countByUserId" resultType="int">
    SELECT COUNT(*)
    FROM ydsz_nw_user_recent
    WHERE user_id = #{userId} AND tenant_id = #{tenantId} AND deleted = FALSE
  </select>

  <!-- 更新节点访问时间（访问记录已存在时） -->
  <update id="updateAccessTime">
    UPDATE ydsz_nw_user_recent
    SET access_type = #{accessType}, accessed_at = NOW(), updated_at = NOW()
    WHERE user_id = #{userId} AND node_id = #{nodeId} AND deleted = FALSE
  </update>

  <!-- 删除用户最早的最近访问记录（超出容量限制时清理） -->
  <delete id="deleteEarliestRecords">
    DELETE FROM ydsz_nw_user_recent
    WHERE user_id = #{userId} AND tenant_id = #{tenantId}
      AND id NOT IN (
        SELECT id FROM ydsz_nw_user_recent
        WHERE user_id = #{userId} AND tenant_id = #{tenantId} AND deleted = FALSE
        ORDER BY accessed_at DESC
        LIMIT #{keepCount}
      )
  </delete>

  <!-- 根据用户ID和节点ID删除访问记录（物理删除） -->
  <delete id="deleteByUserIdAndNodeId">
    DELETE FROM ydsz_nw_user_recent
    WHERE user_id = #{userId} AND node_id = #{nodeId}
  </delete>

</mapper>
