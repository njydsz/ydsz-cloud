<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.njydsz.nextwiki.infra.mapper.UserFavoriteMapper">

  <resultMap id="BaseResultMap" type="com.njydsz.nextwiki.infra.entity.UserFavorite">
    <id column="id" property="id"/>
    <result column="user_id" property="userId"/>
    <result column="node_id" property="nodeId"/>
    <result column="tenant_id" property="tenantId"/>
    <result column="sort_order" property="sortOrder"/>
    <result column="created_at" property="createdAt"/>
    <result column="created_by" property="createdBy"/>
    <result column="updated_at" property="updatedAt"/>
    <result column="updated_by" property="updatedBy"/>
    <result column="deleted" property="deleted"/>
    <result column="deleted_time" property="deletedTime"/>
  </resultMap>

  <sql id="Base_Column_List">
    id, user_id, node_id, tenant_id, sort_order, created_at, created_by, updated_at, updated_by, deleted, deleted_time
  </sql>

  <!-- 查询用户收藏列表（按排序号升序，未删除） -->
  <select id="selectByUserId" resultMap="BaseResultMap">
    SELECT <include refid="Base_Column_List"/>
    FROM ydsz_wiki_user_favorite
    WHERE user_id = #{userId} AND deleted = FALSE
    ORDER BY sort_order ASC, created_at DESC
  </select>

  <!-- 分页查询用户收藏列表 -->
  <select id="selectByUserIdWithPage" resultMap="BaseResultMap">
    SELECT <include refid="Base_Column_List"/>
    FROM ydsz_wiki_user_favorite
    WHERE user_id = #{userId} AND tenant_id = #{tenantId} AND deleted = FALSE
    ORDER BY sort_order ASC, created_at DESC
    LIMIT #{limit} OFFSET #{offset}
  </select>

  <!-- 统计用户收藏数量 -->
  <select id="countByUserId" resultType="int">
    SELECT COUNT(*)
    FROM ydsz_wiki_user_favorite
    WHERE user_id = #{userId} AND tenant_id = #{tenantId} AND deleted = FALSE
  </select>

  <!-- 查询用户的最大排序号 -->
  <select id="selectMaxSortOrder" resultType="int">
    SELECT COALESCE(MAX(sort_order), 0)
    FROM ydsz_wiki_user_favorite
    WHERE user_id = #{userId} AND tenant_id = #{tenantId} AND deleted = FALSE
  </select>

  <!-- 检查节点是否已被用户收藏 -->
  <select id="existsByUserIdAndNodeId" resultType="boolean">
    SELECT COUNT(*) > 0
    FROM ydsz_wiki_user_favorite
    WHERE user_id = #{userId} AND node_id = #{nodeId} AND tenant_id = #{tenantId} AND deleted = FALSE
  </select>

  <!-- 根据用户ID和节点ID删除收藏记录（物理删除） -->
  <delete id="deleteByUserIdAndNodeId">
    DELETE FROM ydsz_wiki_user_favorite
    WHERE user_id = #{userId} AND node_id = #{nodeId}
  </delete>

  <!-- 更新排序号 -->
  <update id="updateSortOrder">
    UPDATE ydsz_wiki_user_favorite
    SET sort_order = #{sortOrder}, updated_at = NOW()
    WHERE user_id = #{userId} AND node_id = #{nodeId} AND deleted = FALSE
  </update>

</mapper>
