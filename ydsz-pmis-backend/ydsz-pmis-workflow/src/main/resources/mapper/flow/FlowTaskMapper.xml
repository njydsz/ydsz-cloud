<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.njydsz.pmis.workflow.mapper.FlowTaskMapper">

    <resultMap id="BaseResultMap" type="com.njydsz.pmis.workflow.entity.FlowTaskDO">
        <id column="id" property="id"/>
        <result column="instance_id" property="instanceId"/>
        <result column="flow_code" property="flowCode"/>
        <result column="definition_id" property="definitionId"/>
        <result column="node_code" property="nodeCode"/>
        <result column="node_name" property="nodeName"/>
        <result column="node_type" property="nodeType"/>
        <result column="business_type" property="businessType"/>
        <result column="business_id" property="businessId"/>
        <result column="business_no" property="businessNo"/>
        <result column="flow_name" property="flowName"/>
        <result column="title" property="title"/>
        <result column="assignor_id" property="assignorId"/>
        <result column="assignor_name" property="assignorName"/>
        <result column="assignee_type" property="assigneeType"/>
        <result column="assignee_id" property="assigneeId"/>
        <result column="assignee_name" property="assigneeName"/>
        <result column="permission_flag" property="permissionFlag"/>
        <result column="perform_type" property="performType"/>
        <result column="approve_count" property="approveCount"/>
        <result column="approve_finished" property="approveFinished"/>
        <result column="task_status" property="taskStatus"/>
        <result column="comment" property="comment"/>
        <result column="status" property="status"/>
        <result column="created_by" property="createdBy"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_by" property="updatedBy"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="claim_at" property="claimAt"/>
        <result column="finish_at" property="finishAt"/>
        <result column="duration_ms" property="durationMs"/>
        <result column="due_at" property="dueAt"/>
        <result column="priority" property="priority"/>
        <result column="reminder_count" property="reminderCount"/>
        <result column="last_reminded_at" property="lastRemindedAt"/>
        <result column="sla_action" property="slaAction"/>
        <result column="sla_escalated" property="slaEscalated"/>
        <result column="deleted" property="deleted"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="provider_trace_id" property="providerTraceId"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, instance_id, flow_code, definition_id, node_code, node_name, node_type,
        business_type, business_id, business_no, flow_name, title,
        assignor_id, assignor_name, assignee_type, assignee_id, assignee_name, permission_flag,
        perform_type, approve_count, approve_finished, task_status, comment, status,
        created_by, created_at, updated_by, updated_at, claim_at, finish_at, duration_ms, due_at, priority,
        reminder_count, last_reminded_at, sla_action, sla_escalated,
        deleted, tenant_id, provider_trace_id
    </sql>

    <select id="selectByInstanceId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM pmis_flow_task
        WHERE instance_id = #{instanceId}
          AND status = 'ENABLED'
          AND deleted = 0
        ORDER BY id ASC
    </select>

    <select id="selectPendingByInstance" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM pmis_flow_task
        WHERE instance_id = #{instanceId}
          AND task_status = 'PENDING'
          AND status = 'ENABLED'
          AND deleted = 0
        ORDER BY id ASC
    </select>

    <select id="selectPendingByNode" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM pmis_flow_task
        WHERE instance_id = #{instanceId}
          AND node_code = #{nodeCode}
          AND task_status = 'PENDING'
          AND status = 'ENABLED'
          AND deleted = 0
        ORDER BY id ASC
    </select>

    <select id="selectTodoByAssignee" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM pmis_flow_task
        WHERE assignee_id = #{assigneeId}
          AND task_status IN ('PENDING', 'CLAIMED')
          AND status = 'ENABLED'
          AND deleted = 0
        ORDER BY priority DESC, created_at ASC
    </select>

    <!-- P2-17: 真分页查询 -->
    <select id="selectTodoByAssigneePage" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM pmis_flow_task
        WHERE assignee_id = #{assigneeId}
          AND task_status IN ('PENDING', 'CLAIMED')
          AND status = 'ENABLED'
          AND deleted = 0
        ORDER BY priority DESC, created_at ASC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countTodoByAssignee" resultType="long">
        SELECT COUNT(*)
        FROM pmis_flow_task
        WHERE assignee_id = #{assigneeId}
          AND task_status IN ('PENDING', 'CLAIMED')
          AND status = 'ENABLED'
          AND deleted = 0
    </select>

    <select id="selectDoneByAssignee" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM pmis_flow_task
        WHERE assignee_id = #{assigneeId}
          AND task_status IN ('COMPLETED', 'REJECTED', 'SKIPPED', 'CANCELLED', 'TIMEOUT')
          AND status = 'ENABLED'
          AND deleted = 0
        ORDER BY finish_at DESC
    </select>

    <update id="completeTask">
        UPDATE pmis_flow_task
        SET task_status = #{taskStatus},
            comment = #{comment},
            finish_at = #{finishAt},
            duration_ms = #{durationMs},
            updated_by = 0,
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
          AND task_status IN ('PENDING', 'CLAIMED')
          AND deleted = 0
    </update>

    <update id="incrementFinished">
        UPDATE pmis_flow_task
        SET approve_finished = approve_finished + 1,
            updated_by = 0,
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
          AND deleted = 0
    </update>

    <update id="cancelByInstance">
        UPDATE pmis_flow_task
        SET task_status = #{taskStatus},
            updated_by = 0,
            updated_at = CURRENT_TIMESTAMP
        WHERE instance_id = #{instanceId}
          AND task_status IN ('PENDING', 'CLAIMED')
          AND deleted = 0
    </update>

    <!-- P0-1: 取消单个任务（边界事件触发时使用） -->
    <update id="cancelTask">
        UPDATE pmis_flow_task
        SET task_status = #{taskStatus},
            comment = #{comment},
            finish_at = CURRENT_TIMESTAMP,
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
          AND task_status IN ('PENDING', 'CLAIMED')
          AND deleted = 0
    </update>

    <update id="skipByNode">
        UPDATE pmis_flow_task
        SET task_status = #{taskStatus},
            updated_by = 0,
            updated_at = CURRENT_TIMESTAMP
        WHERE instance_id = #{instanceId}
          AND node_code = #{nodeCode}
          AND task_status = 'PENDING'
          AND deleted = 0
    </update>

    <!-- P2-18: 冻结某实例下所有 PENDING/CLAIMED 任务（流程挂起时调用） -->
    <update id="freezeByInstance">
        UPDATE pmis_flow_task
        SET task_status = 'FROZEN',
            updated_by = 0,
            updated_at = CURRENT_TIMESTAMP
        WHERE instance_id = #{instanceId}
          AND task_status IN ('PENDING', 'CLAIMED')
          AND deleted = 0
    </update>

    <!-- P2-18: 解冻某实例下所有 FROZEN 任务（流程激活时调用，回到 PENDING） -->
    <update id="unfreezeByInstance">
        UPDATE pmis_flow_task
        SET task_status = 'PENDING',
            updated_by = 0,
            updated_at = CURRENT_TIMESTAMP
        WHERE instance_id = #{instanceId}
          AND task_status = 'FROZEN'
          AND deleted = 0
    </update>

    <!-- P0-5: 统计某实例某节点的未完成任务数（用于并行网关 join 判断） -->
    <select id="countPendingByNode" resultType="int">
        SELECT COUNT(1)
        FROM pmis_flow_task
        WHERE instance_id = #{instanceId}
          AND node_code = #{nodeCode}
          AND task_status IN ('PENDING', 'CLAIMED')
          AND status = 'ENABLED'
          AND deleted = 0
    </select>

    <!-- P0-1: 更新会签计数（设置 approveFinished） -->
    <update id="updateApproveFinished">
        UPDATE pmis_flow_task
        SET approve_finished = #{approveFinished},
            updated_by = 0,
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
          AND deleted = 0
    </update>

    <!-- P1-12: 更新任务办理人信息（用于顺序会签切换办理人） -->
    <update id="updateAssignee">
        UPDATE pmis_flow_task
        SET assignee_id = #{assigneeId},
            assignee_name = #{assigneeName},
            assignee_type = #{assigneeType},
            updated_by = 0,
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
          AND deleted = 0
    </update>

    <!-- P2-32: 查询超期任务（dueAt < now 且状态为 PENDING/CLAIMED） -->
    <select id="selectOverdue" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM pmis_flow_task
        WHERE due_at &lt; CURRENT_TIMESTAMP
          AND task_status IN ('PENDING', 'CLAIMED')
          AND status = 'ENABLED'
          AND deleted = 0
          <if test="assigneeId != null and assigneeId != ''">AND assignee_id = #{assigneeId}</if>
          <if test="tenantId != null">AND tenant_id = #{tenantId}</if>
        ORDER BY due_at ASC
    </select>

    <!-- P2-32: 统计超期任务数量 -->
    <select id="countOverdue" resultType="long">
        SELECT COUNT(*)
        FROM pmis_flow_task
        WHERE due_at &lt; CURRENT_TIMESTAMP
          AND task_status IN ('PENDING', 'CLAIMED')
          AND status = 'ENABLED'
          AND deleted = 0
          <if test="assigneeId != null and assigneeId != ''">AND assignee_id = #{assigneeId}</if>
          <if test="tenantId != null">AND tenant_id = #{tenantId}</if>
    </select>

    <!-- P1-6: SLA 扫描候选 — 所有设置过 dueAt 且未完成的任务 -->
    <select id="selectSlaCandidates" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM pmis_flow_task
        WHERE due_at IS NOT NULL
          AND task_status IN ('PENDING', 'CLAIMED')
          AND status = 'ENABLED'
          AND deleted = 0
        ORDER BY due_at ASC
        LIMIT #{limit}
    </select>

    <!-- P1-6: 增加 SLA 催办计数 -->
    <update id="incrementReminderCount">
        UPDATE pmis_flow_task
        SET reminder_count = #{reminderCount},
            last_reminded_at = #{lastRemindedAt},
            updated_by = 0,
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
          AND deleted = 0
    </update>

    <!-- P1-6: 标记 SLA 动作（用于审计） -->
    <update id="markSlaAction">
        UPDATE pmis_flow_task
        SET sla_action = #{slaAction},
            sla_escalated = #{slaEscalated},
            updated_by = 0,
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
          AND deleted = 0
    </update>
</mapper>
