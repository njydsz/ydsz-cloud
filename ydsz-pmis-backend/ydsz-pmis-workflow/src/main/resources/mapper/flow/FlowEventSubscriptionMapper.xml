<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.njydsz.pmis.workflow.mapper.FlowEventSubscriptionMapper">

    <resultMap id="BaseResultMap" type="com.njydsz.pmis.workflow.entity.FlowEventSubscriptionDO">
        <id column="id" property="id"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="instance_id" property="instanceId"/>
        <result column="definition_id" property="definitionId"/>
        <result column="flow_code" property="flowCode"/>
        <result column="node_code" property="nodeCode"/>
        <result column="node_name" property="nodeName"/>
        <result column="event_type" property="eventType"/>
        <result column="event_ref" property="eventRef"/>
        <result column="correlation_key" property="correlationKey"/>
        <result column="boundary_task_id" property="boundaryTaskId"/>
        <result column="subscription_status" property="subscriptionStatus"/>
        <result column="payload" property="payload"/>
        <result column="triggered_at" property="triggeredAt"/>
        <result column="trigger_source" property="triggerSource"/>
        <result column="cancel_reason" property="cancelReason"/>
        <result column="status" property="status"/>
        <result column="created_by" property="createdBy"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_by" property="updatedBy"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="deleted" property="deleted"/>
        <result column="provider_trace_id" property="providerTraceId"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, tenant_id, instance_id, definition_id, flow_code, node_code, node_name,
        event_type, event_ref, correlation_key, boundary_task_id, subscription_status,
        payload, triggered_at, trigger_source, cancel_reason,
        status, created_by, created_at, updated_by, updated_at, deleted, provider_trace_id
    </sql>

    <!-- 按事件类型 + 引用匹配 WAITING 订阅 -->
    <select id="selectWaitingByEvent" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM pmis_flow_event_subscription
        WHERE tenant_id = #{tenantId}
          AND event_type = #{eventType}
          AND event_ref = #{eventRef}
          AND subscription_status = 'WAITING'
          AND deleted = 0
        ORDER BY created_at ASC
    </select>

    <!-- 按关联键匹配 WAITING 消息订阅 -->
    <select id="selectWaitingByCorrelation" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM pmis_flow_event_subscription
        WHERE tenant_id = #{tenantId}
          AND correlation_key = #{correlationKey}
          AND subscription_status = 'WAITING'
          AND deleted = 0
        ORDER BY created_at ASC
    </select>

    <!-- 标记订阅已触发 -->
    <update id="markTriggered">
        UPDATE pmis_flow_event_subscription
        SET subscription_status = 'COMPLETED',
            payload = #{payload},
            trigger_source = #{triggerSource},
            triggered_at = #{triggeredAt},
            updated_at = NOW()
        WHERE id = #{id}
          AND subscription_status = 'WAITING'
    </update>

    <!-- 取消某 userTask 关联的所有边界事件订阅 -->
    <update id="cancelByTask">
        UPDATE pmis_flow_event_subscription
        SET subscription_status = 'CANCELLED',
            cancel_reason = #{reason},
            updated