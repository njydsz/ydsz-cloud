<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.njydsz.pmis.workflow.mapper.definition.FlowTemplateMapper">

    <resultMap id="BaseResultMap" type="com.njydsz.pmis.workflow.entity.definition.FlowTemplateDO">
        <id column="id" property="id"/>
        <result column="template_code" property="templateCode"/>
        <result column="template_name" property="templateName"/>
        <result column="category" property="category"/>
        <result column="description" property="description"/>
        <result column="icon" property="icon"/>
        <result column="bpmn_xml" property="bpmnXml"/>
        <result column="form_path" property="formPath"/>
        <result column="use_count" property="useCount"/>
        <result column="sort_order" property="sortOrder"/>
        <!-- P2-9: 模板继承与版本化字段 -->
        <result column="parent_template_id" property="parentTemplateId"/>
        <result column="version" property="version"/>
        <result column="version_label" property="versionLabel"/>
        <result column="inherit_type" property="inheritType"/>
        <result column="is_latest" property="isLatest"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, template_code, template_name, category, description, icon, bpmn_xml,
        form_path, use_count, sort_order,
        parent_template_id, version, version_label, inherit_type, is_latest
    </sql>

    <!-- P2-9: selectByCategory 仅返回 is_latest=1 的最新版本，保持向后兼容 -->
    <select id="selectByCategory" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM pmis_flow_template
        WHERE deleted = 0
          AND is_latest = 1
        <if test="category != null and category != ''">
            AND category = #{category}
        </if>
        ORDER BY sort_order ASC, id ASC
    </select>

    <!-- P2-9: selectByTemplateCode 仅返回 is_latest=1 的最新版本 -->
    <select id="selectByTemplateCode" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM pmis_flow_template
        WHERE template_code = #{templateCode}
          AND deleted = 0
          AND is_latest = 1
    </select>

    <update id="incrementUseCount">
        UPDATE pmis_flow_template
        SET use_count = use_count + 1
        WHERE template_code = #{templateCode}
          AND deleted = 0
          AND is_latest = 1
    </update>

    <!-- P2-9: 查询某 template_code 的全部历史版本（最新版本在首位） -->
    <select id="selectVersionsByTemplateCode" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM pmis_flow_template
        WHERE template_code = #{templateCode}
          AND deleted = 0
        ORDER BY version DESC, id ASC
    </select>

    <!-- P2-9: 按父模板 ID 查询继承关系 -->
    <select id="selectByParentTemplateId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM pmis_flow_template
        WHERE parent_template_id = #{parentTemplateId}
          AND deleted = 0
          AND is_latest = 1
        ORDER BY sort_order ASC, id ASC
    </select>

    <!-- P2-9: 将指定 template_code 的所有版本标记为非最新 -->
    <update id="markAsNotLatest">
        UPDATE pmis_flow_template
        SET is_latest = 0,
            updated_at = CURRENT_TIMESTAMP
        WHERE template_code = #{templateCode}
          AND deleted = 0
          AND is_latest = 1
    </update>

    <!-- P2-9: 查询某 template_code 当前的最大版本号 -->
    <select id="selectMaxVersion" resultType="java.lang.Integer">
        SELECT MAX(version)
        FROM pmis_flow_template
        WHERE template_code = #{templateCode}
          AND deleted = 0
    </select>

</mapper>
