<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.njydsz.pmis.workflow.mapper.integration.FlowUserMapper">

    <resultMap id="BaseResultMap" type="com.njydsz.pmis.workflow.entity.FlowUserDO">
        <id column="id" property="id"/>
        <result column="task_id" property="taskId"/>
        <result column="instance_id" property="instanceId"/>
        <result column="node_code" property="nodeCode"/>
        <result column="user_type" property="userType"/>
        <result column="user_id" property="userId"/>
        <result column="user_name" property="userName"/>
        <result column="processed" property="processed"/>
        <result column="process_at" property="processAt"/>
        <result column="comment" property="comment"/>
        <result column="status" property="status"/>
        <result column="created_by" property="createdBy"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_by" property="updatedBy"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="deleted" property="deleted"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="provider_trace_id" property="providerTraceId"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, task_id, instance_id, node_code, user_type, user_id, user_name, processed, process_at,
        comment, status,
        created_by, created_at, updated_by, updated_at, deleted, tenant_id, provider_trace_id
    </sql>

    <select id="selectByTaskId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM pmis_flow_user
        WHERE task_id = #{taskId}
          AND status = 'ENABLED'
          AND deleted = 0
        ORDER BY id ASC
    </select>

    <update id="markProcessed">
        UPDATE pmis_flow_user
        SET processed = 1,
            process_at = #{processAt},
            comment = #{comment},
            updated_by = 0,
            updated_at = CURRENT_TIMESTAMP
        WHERE task_id = #{taskId}
          AND user_id = #{userId}
          AND processed = 0
          AND deleted = 0
    </update>

    <!-- P1-12: 查某实例某节点未处理的用户（顺序会签场景下取下一个办理人） -->
    <select id="selectUnprocessedByInstanceAndNode" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM pmis_flow_user
        WHERE instance_id = #{instanceId}
          AND node_code = #{nodeCode}
          AND processed = 0
          AND status = 'ENABLED'
          AND deleted = 0
        ORDER BY id ASC
    </select>

    <!-- P0-4: 查某用户待办关联的任务 ID（通过 pmis_flow_user 表） -->
    <select id="selectTaskIdsByUser" resultType="java.lang.Long">
        SELECT DISTINCT task_id
        FROM pmis_flow_user
        WHERE user_id = #{userId}
          AND processed = 0
          AND status = 'ENABLED'
          AND deleted = 0
          AND tenant_id = #{tenantId}
    </select>

    <!-- 批量插入 -->
    <insert id="batchInsert">
        INSERT INTO pmis_flow_user
            (task_id, instance_id, node_code, user_type, user_id, user_name,
             processed, process_at, comment, status, created_by, created_at,
             updated_by, updated_at, deleted, tenant_id, provider_trace_id)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.taskId}, #{item.instanceId}, #{item.nodeCode}, #{item.userType},
             #{item.userId}, #{item.userName}, #{item.processed}, #{item.processAt},
             #{item.comment}, 'ENABLED', 0, CURRENT_TIMESTAMP, 0, CURRENT_TIMESTAMP, 0,
             #{item.tenantId}, #{item.providerTraceId})
        </foreach>
    </insert>
</mapper>
