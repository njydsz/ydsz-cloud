<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.njydsz.pmis.workflow.mapper.instance.FlowInboxMapper">

    <select id="selectInboxByUser" resultType="com.njydsz.pmis.workflow.entity.FlowInboxDO">
        SELECT * FROM pmis_flow_inbox
        WHERE receiver_id = #{receiverId}
          AND deleted = 0
          <if test="tenantId != null">AND tenant_id = #{tenantId}</if>
          <if test="onlyUnread">AND read_status = false</if>
        ORDER BY created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countInbox" resultType="long">
        SELECT COUNT(1) FROM pmis_flow_inbox
        WHERE receiver_id = #{receiverId}
          AND deleted = 0
          <if test="tenantId != null">AND tenant_id = #{tenantId}</if>
          <if test="onlyUnread">AND read_status = false</if>
    </select>

    <update id="batchMarkRead">
        UPDATE pmis_flow_inbox
        SET read_status = true,
            read_at = NOW(),
            updated_at = NOW()
        WHERE receiver_id = #{receiverId}
          AND deleted = 0
          AND read_status = false
          <if test="tenantId != null">AND tenant_id = #{tenantId}</if>
          AND id IN
          <foreach collection="ids" item="id" open="(" separator="," close=")">
              #{id}
          </foreach>
    </update>

</mapper>
