<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.njydsz.pmis.workflow.infra.mapper.FlowDmnDecisionMapper">

    <select id="selectPublishedByCode" resultType="com.njydsz.pmis.workflow.domain.entity.FlowDmnDecisionDO">
        SELECT * FROM pmis_flow_dmn_decision
        WHERE decision_code = #{decisionCode}
          AND tenant_id = #{tenantId}
          AND status = 'PUBLISHED'
          AND deleted = 0
        ORDER BY decision_version DESC
        LIMIT 1
    </select>

    <select id="selectByNode" resultType="com.njydsz.pmis.workflow.domain.entity.FlowDmnDecisionDO">
        SELECT * FROM pmis_flow_dmn_decision
        WHERE flow_code = #{flowCode}
          AND node_code = #{nodeCode}
          AND tenant_id = #{tenantId}
          AND status = 'PUBLISHED'
          AND deleted = 0
        ORDER BY decision_version DESC
        LIMIT 1
    </select>

    <select id="selectPublishedList" resultType="com.njydsz.pmis.workflow.domain.entity.FlowDmnDecisionDO">
        SELECT * FROM pmis_flow_dmn_decision
        WHERE tenant_id = #{tenantId}
          AND status = 'PUBLISHED'
          AND deleted = 0
        <if test="decisionCode != null and decisionCode != ''">
            AND decision_code LIKE CONCAT('%', #{decisionCode}, '%')
        </if>
        ORDER BY updated_at DESC
    </select>

</mapper>
