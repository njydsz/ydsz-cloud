/**
 * Agent 模块 - 业务枚举层。
 *
 * <p>Agent 模块的枚举常量：
 * <ul>
 *   <li>{@code AgentType}      - Agent 类型（RISK / PROFIT / RESOURCE 等）</li>
 *   <li>{@code AgentRunStatus} - Agent 运行状态（RUNNING / SUCCESS / FAILED / TIMEOUT）</li>
 *   <li>{@code AgentAlertLevel}- Agent 告警等级（INFO / WARN / CRITICAL）</li>
 * </ul>
 *
 * @author ydsz-pmis-team
 * @since 1.0.0
 */
package com.njydsz.pmis.agent.enums;
