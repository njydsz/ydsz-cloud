<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.njydsz.pmis.project.mapper.opportunity.OpportunityMapper">

    <resultMap id="BaseResultMap" type="com.njydsz.pmis.project.entity.OpportunityDO">
        <id column="id" property="id"/>
        <result column="opportunity_code" property="opportunityCode"/>
        <result column="opportunity_name" property="opportunityName"/>
        <result column="customer_id" property="customerId"/>
        <result column="customer_name" property="customerName"/>
        <result column="business_dept_id" property="businessDeptId"/>
        <result column="owner_id" property="ownerId"/>
        <result column="owner_name" property="ownerName"/>
        <result column="level" property="level"/>
        <result column="source" property="source"/>
        <result column="industry" property="industry"/>
        <result column="estimated_amount" property="estimatedAmount"/>
        <result column="win_rate" property="winRate"/>
        <result column="expected_sign_date" property="expectedSignDate"/>
        <result column="expected_start_date" property="expectedStartDate"/>
        <result column="expected_end_date" property="expectedEndDate"/>
        <result column="status" property="status"/>
        <result column="lost_reason" property="lostReason"/>
        <result column="competitor" property="competitor"/>
        <result column="remark" property="remark"/>
        <result column="tags" property="tags"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="created_by" property="createdBy"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_by" property="updatedBy"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <select id="selectByCode" resultMap="BaseResultMap">
        SELECT id, opportunity_code, opportunity_name, customer_id, customer_name, business_dept_id, owner_id, owner_name, level, source, industry, estimated_amount, win_rate, expected_sign_date, expected_start_date, expected_end_date, status, lost_reason, competitor, remark, tags, tenant_id, created_by, created_at, updated_by, updated_at, deleted FROM pmis_project_opportunity
        WHERE opportunity_code = #{code} AND deleted = 0
        LIMIT 1
    </select>

    <update id="updateStatus">
        UPDATE pmis_project_opportunity
        SET status = #{status},
            lost_reason = COALESCE(#{lostReason}, lost_reason),
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id} AND deleted = 0
    </update>

    <select id="aggregateByStatus" resultType="java.util.Map">
        SELECT status, COUNT(1) AS cnt, COALESCE(SUM(estimated_amount), 0) AS amount
        FROM pmis_project_opportunity
        WHERE tenant_id = #{tenantId} AND deleted = 0
        GROUP BY status
    </select>

    <select id="aggregateByLevel" resultType="java.util.Map">
        SELECT level, COUNT(1) AS cnt, COALESCE(SUM(estimated_amount), 0) AS amount
        FROM pmis_project_opportunity
        WHERE tenant_id = #{tenantId} AND deleted = 0
        GROUP BY level
    </select>

    <select id="countByStatus" resultType="java.lang.Long">
        SELECT COUNT(1) FROM pmis_project_opportunity
        WHERE status = #{status} AND tenant_id = #{tenantId} AND deleted = 0
    </select>

</mapper>
