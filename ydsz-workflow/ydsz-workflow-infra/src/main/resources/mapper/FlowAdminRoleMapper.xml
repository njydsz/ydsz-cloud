<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.njydsz.workflow.infra.mapper.FlowAdminRoleMapper">

    <select id="selectByUserId" resultType="com.njydsz.workflow.infra.entity.FlowAdminRole">
        SELECT * FROM ydsz_flow_admin_role
        WHERE user_id = #{userId}
          AND deleted = 0
          <if test="tenantId != null">AND tenant_id = #{tenantId}</if>
        ORDER BY created_at DESC
    </select>

    <select id="selectByUserAndRole" resultType="com.njydsz.workflow.infra.entity.FlowAdminRole">
        SELECT * FROM ydsz_flow_admin_role
        WHERE user_id = #{userId}
          AND role_code = #{roleCode}
          AND deleted = 0
          <if test="tenantId != null">AND tenant_id = #{tenantId}</if>
        LIMIT 1
    </select>

</mapper>
