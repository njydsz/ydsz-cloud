<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.remisoft.workflow.infra.mapper.FlowThirdPartyLogMapper">

    <!-- 更新处理状态与错误信息 -->
    <update id="updateStatus">
        UPDATE remi_flow_third_party_log
        SET handle_status = #{status},
            error_msg     = #{errorMsg}
        WHERE id = #{id}
    </update>

    <!-- P0-4: 更新重试结果（同时累加重试次数、更新最后重试时间） -->
    <update id="updateRetryResult">
        UPDATE remi_flow_third_party_log
        SET handle_status  = #{status},
            error_msg      = #{errorMsg},
            retry_count    = #{retryCount},
            last_retried_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

</mapper>
