<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.remisoft.workflow.infra.mapper.FlowHisInstanceMapper">

    <resultMap id="BaseResultMap" type="com.remisoft.workflow.domain.entity.FlowHisInstance">
        <id column="id" property="id"/>
        <result column="flow_code" property="flowCode"/>
        <result column="flow_name" property="flowName"/>
        <result column="definition_id" property="definitionId"/>
        <result column="flow_version" property="flowVersion"/>
        <result column="business_type" property="businessType"/>
        <result column="business_id" property="businessId"/>
        <result column="business_no" property="businessNo"/>
        <result column="title" property="title"/>
        <result column="initiator_id" property="initiatorId"/>
        <result column="initiator_name" property="initiatorName"/>
        <result column="current_node_code" property="currentNodeCode"/>
        <result column="current_node_name" property="currentNodeName"/>
        <result column="variable" property="variable"/>
        <result column="flow_status" property="flowStatus"/>
        <result column="activity_status" property="activityStatus"/>
        <result column="start_at" property="startAt"/>
        <result column="end_at" property="endAt"/>
        <result column="duration_ms" property="durationMs"/>
        <result column="created_by" property="createdBy"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_by" property="updatedBy"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="archived_at" property="archivedAt"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="provider_trace_id" property="providerTraceId"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, flow_code, flow_name, definition_id, flow_version,
        business_type, business_id, business_no, title,
        initiator_id, initiator_name, current_node_code, current_node_name,
        variable, flow_status, activity_status,
        start_at, end_at, duration_ms,
        created_by, created_at, updated_by, updated_at,
        archived_at, tenant_id, provider_trace_id
    </sql>

    <insert id="batchInsert">
        INSERT INTO remi_flow_his_instance (
            flow_code, flow_name, definition_id, flow_version,
            business_type, business_id, business_no, title,
            initiator_id, initiator_name, current_node_code, current_node_name,
            variable, flow_status, activity_status,
            start_at, end_at, duration_ms,
            created_by, created_at, updated_by, updated_at,
            archived_at, tenant_id, provider_trace_id
        ) VALUES
        <foreach collection="list" item="it" separator=",">
            (
                #{it.flowCode}, #{it.flowName}, #{it.definitionId}, #{it.flowVersion},
                #{it.businessType}, #{it.businessId}, #{it.businessNo}, #{it.title},
                #{it.initiatorId}, #{it.initiatorName}, #{it.currentNodeCode}, #{it.currentNodeName},
                #{it.variable}, #{it.flowStatus}, #{it.activityStatus},
                #{it.startAt}, #{it.endAt}, #{it.durationMs},
                #{it.createdBy}, #{it.createdAt}, #{it.updatedBy}, #{it.updatedAt},
                COALESCE(#{it.archivedAt}, CURRENT_TIMESTAMP), #{it.tenantId}, #{it.providerTraceId}
            )
        </foreach>
    </insert>

    <delete id="deleteByOriginalIds">
        DELETE FROM remi_flow_instance
        WHERE id IN
        <foreach collection="ids" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </delete>

    <select id="aggregateByTenant" resultType="java.util.Map">
        SELECT
            flow_code,
            tenant_id,
            COUNT(*) AS archived_count,
            MIN(archived_at) AS first_archived_at,
            MAX(archived_at) AS last_archived_at
        FROM remi_flow_his_instance
        <where>
            <if test="tenantId != null">
                AND tenant_id = #{tenantId}
            </if>
        </where>
        GROUP BY flow_code, tenant_id
    </select>

    <select id="selectByArchivedAtBefore" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM remi_flow_his_instance
        WHERE archived_at &lt; #{threshold}
        ORDER BY archived_at ASC
        LIMIT #{limit}
    </select>

</mapper>
