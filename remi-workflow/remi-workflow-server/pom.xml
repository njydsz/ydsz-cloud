<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.remisoft</groupId>
        <artifactId>remi-workflow</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>
    <artifactId>remi-workflow-server</artifactId>
    <name>remi-workflow-server</name>
    <description>DDD Module</description>
    <dependencies>
        <dependency>
            <groupId>com.remisoft</groupId>
            <artifactId>remi-workflow-infra</artifactId>
        </dependency>
        <!-- P2-3: 显式声明 common-util（多文件使用 FileTypeUtils/DateUtils 等工具类） -->
        <dependency>
            <groupId>com.remisoft</groupId>
            <artifactId>remi-common-util</artifactId>
        </dependency>
        <!-- common-web 传递引入: common-base(→core/util/exception), common-auth, common-safe, common-redis -->
        <!-- common-jdbc 传递引入: common-domain -->
        <!-- remi-common-domain: explicit declaration for direct usage (JobHandler/DomainEvent/PageQuery, was transitive via common-jdbc) -->
        <dependency>
            <groupId>com.remisoft</groupId>
            <artifactId>remi-common-domain</artifactId>
        </dependency>
        <!-- remi-cronjob-domain: JobHandler 抽象（FlowTimeout/ThirdPartyRetry/HistoryArchive JobHandler 由 cronjob 框架调度） -->
        <dependency>
            <groupId>com.remisoft</groupId>
            <artifactId>remi-cronjob-domain</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- common-feign 传递引入: spring-cloud-starter-openfeign -->
        <dependency>
            <groupId>com.remisoft</groupId>
            <artifactId>remi-common-jdbc</artifactId>
        </dependency>
                <dependency>
            <groupId>com.remisoft</groupId>
            <artifactId>remi-common-lock</artifactId>
        </dependency>
        <!-- remi-common-cache（流程定义缓存，替代 Caffeine） -->
        <dependency>
            <groupId>com.remisoft</groupId>
            <artifactId>remi-common-cache</artifactId>
        </dependency>
        <dependency>
            <groupId>com.remisoft</groupId>
            <artifactId>remi-common-feign</artifactId>
        </dependency>
        <dependency>
            <groupId>com.remisoft</groupId>
            <artifactId>remi-common-audit</artifactId>
        </dependency>
        <dependency>
            <groupId>com.remisoft</groupId>
            <artifactId>remi-common-file</artifactId>
        </dependency>
        <dependency>
            <groupId>com.remisoft</groupId>
            <artifactId>remi-common-notify</artifactId>
        </dependency>
        <dependency>
            <groupId>com.remisoft</groupId>
            <artifactId>remi-common-queue</artifactId>
        </dependency>
        <!-- P0-2: 事务性 Outbox 事件模式 -->
        <dependency>
            <groupId>com.remisoft</groupId>
            <artifactId>remi-common-event</artifactId>
        </dependency>
        <!-- P0-1: 统一线程池管理（替代手动 ThreadPoolTaskExecutor 创建） -->
        <dependency>
            <groupId>com.remisoft</groupId>
            <artifactId>remi-common-thread</artifactId>
        </dependency>
        <!-- P1-4: 搜索 Provider SPI（流程实例搜索） -->
        <dependency>
            <groupId>com.remisoft</groupId>
            <artifactId>remi-common-search</artifactId>
        </dependency>
        <!-- P0-2: 分布式事务（Seata AT 模式，跨服务事务场景启用） -->
        <dependency>
            <groupId>com.remisoft</groupId>
            <artifactId>remi-common-seata</artifactId>
        </dependency>
        <dependency>
            <groupId>com.remisoft</groupId>
            <artifactId>remi-common-web</artifactId>
        </dependency>
        <!-- remi-common-core: explicit declaration for direct usage (was transitive via common-web) -->
        <dependency>
            <groupId>com.remisoft</groupId>
            <artifactId>remi-common-core</artifactId>
        </dependency>
    
        <dependency>
            <groupId>com.remisoft</groupId>
            <artifactId>remi-literule-api</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.remisoft</groupId>
            <artifactId>remi-message-api</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>com.remisoft</groupId>
            <artifactId>remi-userinfo-api</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.googlecode.aviator</groupId>
            <artifactId>aviator</artifactId>
            <version>5.4.3</version>
        </dependency>
        <!-- Micrometer Prometheus 注册器（FlowMetrics 指标暴露给 /actuator/prometheus） -->
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
        </dependency>
        <!-- dynamic-datasource（@DS 注解，common-jdbc 中标记为 optional） -->
        <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>dynamic-datasource-spring-boot3-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
            <dependency>
            <groupId>com.remisoft</groupId>
            <artifactId>remi-common-json</artifactId>
        </dependency>
        <!-- P0-1: 统一监控告警（Sentry SPI：ELK/Loki 日志推送 + 告警收敛 + SLA） -->
        <dependency>
            <groupId>com.remisoft</groupId>
            <artifactId>remi-common-sentry</artifactId>
        </dependency>
        <!-- P0-2: 多租户隔离 -->
        <dependency>
            <groupId>com.remisoft</groupId>
            <artifactId>remi-common-tenant</artifactId>
        </dependency>
</dependencies>
</project>
