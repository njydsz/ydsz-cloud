<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.njydsz</groupId>
        <artifactId>ydsz-cronjob</artifactId>
        <version>26.09.01-SNAPSHOT</version>
    </parent>
    <artifactId>ydsz-cronjob-api</artifactId>
    <name>ydsz-cronjob-api</name>
    <description>DDD Module</description>
    <dependencies>
        <!--
          P1-2: api 模块的 CronjobServiceClient 实际仅使用 YdszResponse / Map / FeignClientConstants，
          无需直接引用 domain 中的 DTO/VO/Entity。将 domain 依赖降级为 provided，
          避免 api 打包时带入 common-jdbc + MyBatis-Plus 等传递依赖，减小下游接入方 classpath 污染。
        -->
        <dependency>
            <groupId>com.njydsz</groupId>
            <artifactId>ydsz-cronjob-domain</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>com.njydsz</groupId>
            <artifactId>ydsz-common-core</artifactId>
        </dependency>
        <dependency>
            <groupId>com.njydsz</groupId>
            <artifactId>ydsz-common-feign</artifactId>
        </dependency>
    </dependencies>
</project>
